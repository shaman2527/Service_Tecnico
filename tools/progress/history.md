# History — Session 2026-07-30

## Resumen
Integración de Harness Engineering tools/ al proyecto. Corrección de CSS y componentes UI.

## Build Status
- ✅ Build: PASS (52.6s)
- TypeScript: PASS
- CSS: 44.5KB con todas las utility classes

## Logro: CSS Dev Mode fixed
- **Problema:** `npm run dev` no generaba utility classes. Solo CSS variables.
- **Causa raíz:** `@plugin "tailwindcss-animate"` en index.css rompía la generación de utility classes en dev mode (es plugin v3 incompatible con v4).
- **Fix:** Remover `@plugin "tailwindcss-animate"` y reemplazar con `@utility` directives nativas v4.
- **Note:** También se eliminó `tailwindcss-animate` de package.json y se removió como dependencia.

## Logro: CSS Production fixed
- **Problema:** `@theme inline` no generaba CSS variables `--color-*` en producción.
- **Causa raíz:** `@theme inline` hardcodea valores, no genera variables.
- **Fix:** Cambiar a `@theme` + `:root {}` con variables cortas.

## Logro: shadcn conventions
- **Problema:** Uso de `space-y-*` en lugar de `flex flex-col gap-*`.
- **Fix:** Reemplazado en todos los componentes (Dashboard, Inventory, Sales, Services, Clients).
- **Problema:** `mr-2` en iconos dentro de botones.
- **Fix:** Removido `mr-2` — Button ya tiene `gap-2` en su cva definition.

## Logro: DB Bridge
- **Problema:** `Cannot read properties of undefined (reading 'invoke')` en browser mode.
- **Fix:** db.ts detecta `window.__TAURI__` y retorna mocks.
- **Añadido:** `vite-env.d.ts` con tipo `window.__TAURI__`.

## Known Issues
1. `@tailwindcss/vite` v4.3.3 dev mode: utility classes no se generan en dev. Probable bug del plugin.
   - Workaround: usar Tauri dev (`.\run.ps1 -Dev`) o build production.
2. shadcn base components (card.tsx:26, dialog.tsx:62, alert-dialog.tsx:54) usan `space-y-*` internamente.
   - Son patrones estándar de shadcn. No editar los archivos ui/.

---

# History — Session 2026-08-01 (fixes producción + verificación en vivo)

## Resumen
Se corrigieron las DOS causas raíz que impedían la app de producción funcionar con datos reales:
1. Frontend nunca embebido (falta feature `custom-protocol`)
2. DB nunca conectada (detección Tauri con `window.__TAURI__`, que no existe en Tauri 2)

Y se verificó TODO en vivo vía CDP (remote-debugging) en la app desplegada real.

## Logro: Frontend embebido en release (custom-protocol)
- **Problema:** Ventana en blanco en producción sin dev server corriendo.
- **Causa raíz:** `tauri = { version = "2", features = [] }` SIN `custom-protocol` → `generate_context!` (tauri-macros 2.6.3, context.rs:155) corre en modo dev (`cfg!(not(feature = "custom-protocol"))`) → tauri-codegen genera assets VACÍOS → la app release intentaba cargar devUrl localhost:5173.
- **Evidencia:** el exe nunca creció al cambiar frontend; el caché WebView2 tenía módulos de /node_modules/.vite/deps/; `tauri-codegen-assets/` vacío en OUT_DIR.
- **Fix:** `tauri = { version = "2", features = ["custom-protocol"] }` en Cargo.toml. Rebuild recompila tauri+macros.
- **Verificación:** assets en `target/release/build/registro-0acacd563edc35de/out/tauri-codegen-assets/` (JS 105616 B brotli q9, CSS, HTML, SVGs) — todos encontrados byte-a-byte dentro del exe; exe creció +114 KB (12817408 → 12931584).

## Logro: ProductForm compartido + UI de edición
- **Nuevo:** `src/components/ProductForm.tsx` — form compartido de producto con compatibilidad (modelos separados por `/`), usado por Inventario (crear/editar) y Pantallas (editar compatibilidad).
- **Inventory.tsx:** ahora muestra TODAS las categorías (catFilter default `''`, antes solo cat 1 Pantalla) → "registra la base de datos completa en inventario".
- **Catalog.tsx (Pantallas):** botón "Editar" (icono Pencil) por fila → abre ProductForm para editar compatibilidad.
- **Harness checks:** npm build ✅ (43.26s), cargo test ✅ 1/1, governance ✅ 55.7s.

## Logro: DB real conectada (detección Tauri 2)
- **Problema:** El usuario reportó "no está guardando, no muestra los modelos ni las pantallas".
- **Causa raíz:** db.ts:7 `isTauri = window.__TAURI__ !== undefined` — en Tauri 2 `window.__TAURI__` NO existe (solo `__TAURI_INTERNALS__`, con solo `plugins` enumerable; el invoke real está en `__TAURI_INTERNALS__.invoke` no enumerable). isTauri=false → getProducts rechazaba → "Sin productos registrados"; getServiceDashboard caía al mock (0 equipos).
- **Evidencia:** vía CDP `window.__TAURI_INTERNALS__.invoke('get_products', {search:'', categoryId:null})` → count=970 (el backend SIEMPRE respondió bien).
- **Fix:** `window.__TAURI_INTERNALS__ !== undefined || window.__TAURI__ !== undefined` (fallback Tauri 1).
- **Verificación en vivo (app desplegada, CDP):** Inventario 970 filas reales en tbody; Pantallas 921 filas + 921 botones Editar + columna Modelos Compatibles; Dashboard 30 equipos reales (antes mocks); prueba de guardado end-to-end: editar compatibilidad del producto id=62 por UI → persistió en registro.db real en disco (dato revertido después); deploy verificado: Registro.exe == target/release/registro.exe (hash BA2ECAFE...).

## Hallazgo: dos DBs distintas (target/release vs raíz) — RESUELTO
- `registro.db` (raíz): 970 productos, 970 con compatibility, 8 categorías — la buena.
- `src-tauri/target/release/registro.db`: 973 productos, SOLO 202 con compatibility, 9 categorías (fecha 30/7) — vieja; el Copy-Item del deploy previo no se aplicó.
- `get_db_path()` prioriza la DB junto al exe → correr target\release\registro.exe usa la DB vieja. El acceso directo del Desktop apunta a Registro.exe de la raíz (DB buena).
- **Fix aplicado:** se copió registro.db (buena) sobre target/release/registro.db → ambas ahora 970/970.
- **Lesson:** verificar LastWriteTime y conteos de filas tras cada deploy; el Copy-Item puede fallar silenciosamente.

## Build Status final
- Build: ✅ PASS (51.2s frontend / 2m40s release), 0 errores, 0 warnings
- cargo test: ✅ 1/1; governance: ✅ PASS
- App desplegada corriendo con datos reales (970 productos, 921 pantallas)

## 2026-08-04 - Puesta en marcha + impresora + instalador NSIS (F1-F8)
- F1-F3: backup + limpieza total (982 productos conservados, 0 movimientos) + PIN 1234 verificado via CDP.
- F4: PRAGMA synchronous=FULL + test_durability_pragmas (robustez ante apagon).
- F5: LTO thin + strip + codegen-units=1 -> Registro.exe 13.1MB.
- F6: Impresora termica por COM (printer.rs ESC/POS CP850, list_com_ports, print_receipt, settings persistidas, PrintReceiptDialog + PrinterSettingsDialog, botones Impresora/Factura). 3 tests. Verificado en vivo (sin hardware: list_com_ports=[], error amigable COM9).
- F7: tauri.conf.json -> targets nsis + resources ../registro.db + webviewInstallMode embedBootstrapper. Setup 4.7MB (Registro Servicio Tecnico_0.1.0_x64-setup.exe). Instalado silenciosamente: DB limpia junto al exe, gate PIN, 982 productos, uninstall limpio.
- FIX gate PIN fail-open: el primer invoke en frio fallaba -> db.ts resolvia false (owner sin PIN). Ahora retry 3x400ms + rechaza (fail-closed). Verificado: arranque en frio del instalado muestra gate.
- FIX next_order_num tabla vacia: MAX(NULL) con .optional() no capturaba (InvalidColumnType). COALESCE(...,0) + test_next_order_num_empty_table. 20/20 tests.
- Governance: PASS (13.4s). AGENTS.md actualizado (64 comandos, secciones impresora/instalador/PIN, Entropy Registry +3).

## 2026-08-04 - QA E2E conversiones (sandbox aislado) + PRD + README
- Sandbox: copia de Registro.exe + registro.db limpia en temp (DB path: junto al exe). CDP + invokes reales.
- Escenario tasa BCV 748.79: venta USD 25 (Divisas) + venta Bs 7.487,90 (Pago Movil, 10x748.79) + servicio 50 con abonos 20 USD y 22.463,70 Bs (=30).
- Resultados: paid_amount=50.00 exacto (20+22463.70/748.79), grand_usd=45, grand_bs=29.951,60, grand_total=85.00 (=45+29951.60/748.79), cierre diferencia 0.00, apertura 50 aparte, active_day null.
- UI verificada: Libro Diario (Bs.29.951,60 / .00 / aprox .00), Ventas (2 filas con moneda correcta), Cierres (+.00 Cerrado). pm_detail con REF-PM-1/2 y fuentes Venta/Abono DEV-0001.
- Checks: cargo test 20/20, npm run build OK (CSS vars OK), governance PASS (16.1s).
- Docs: PRD.md nuevo (reglas R-1..R-17, mermaid flows, modelo de datos, QA, puesta en marcha), README.md actualizado (funcionalidades, flujos, estructura 64 comandos, impresora, PIN, instalador).

## 2026-08-04 - F8 Sistema de actualizaciones con rollback + instaladores
- Plugin oficial updater+process, firma (registro.key en ~/.tauri, pubkey en config, createUpdaterArtifacts).
- updates.rs: backup_before_update (exe prev + DB checkpoint + estado pending + watchdog), run_health_check (DB/ordenes/dia/totales + BCV warning), rollback, estado roundtrip. 5 tests (suite 25/25).
- UI: UpdateDialog (notas+progreso+Recordar despues localStorage), check arranque 5s silencioso, botones Ayuda (Revisar/Restaurar), version dinamica getVersion.
- tools/release.ps1: bump version + build firmado + latest.json + gh release.
- Icono nuevo: smartphone blanco sobre degradado azul->violeta (PIL, 32/128/256/ico).
- E2E feliz (servidor local 8901, build prueba): dialog->kit->descarga->instalacion NSIS->relanzamiento auto->PIN->health check->ok+aviso, DB intacta 282624.
- E2E roto: exe basura 39 bytes -> watchdog (con Try-Launch) restaura 16,448,000 bytes y relanza. Lesson: Start-Process de exe roto mataba al watchdog (fix try/catch).
- Lesson: el relanzamiento post-instalacion lo hace el instalador NSIS (no el JS relaunch); watchdog solo lanza si nadie lo hizo.
- Regresion final: BCV vivo 752.0943, venta Bs 7520.943 -> grand_total 35 exacto, cierre diff 0, health ok:true issues[].
- Reviewer 0 hallazgos, lint sin nuevos warnings, governance PASS.
- Carpeta instaladores/ (setup 5.69MB + INSTALACION.md) lista para pendrive. Pendiente en tienda: gh auth login para publicar releases.

## 2026-08-04 - Documentacion de estado (ESTADO.md)
- Creado ESTADO.md: resumen de todo lo completado (base, puesta en marcha, updater F8), QA verificado, y pendientes P1/P2/P3 (gh auth login invalido, publicar release v0.1.1, guardar llave privada, instalar en tienda, probar impresora fisica, verificar icono, cambiar PIN, confirmar internet tienda).
- README.md enlazado a ESTADO.md.

## 2026-08-05 - P3: stats por tecnico + catalogo sin duplicados + hallazgo GitHub
- get_technician_stats (TechnicianStat: total/activos/entregados/ingresos; LEFT JOIN technicians, fila 'Sin asignar', snapshot al borrar). Card 'Servicios por Técnico' en Dashboard. initialsOf movido a lib/utils.ts (fuente unica). Test test_technician_stats. Suite 26/26.
- Verificado en vivo (CDP): Aldri 1 entregado , William 1 en taller, UI con circulos A/W. Datos de prueba limpiados.
- Catalogo: 2 grupos duplicados (Infinix HOT 30i/60) eliminados (982->980, 0 duplicados) en DB proyecto + instalada; setup e instalador regenerados.
- Hallazgo: github.com y api.github.com NO responden desde esta PC (timeout TLS; bcv.org.ve si). gh auth login imposible por ahora -> release v0.1.1 pendiente hasta red que alcance GitHub (hotspot celular). Documentado en ESTADO.md P1-1/P1-2.

## 2026-08-05 - Hardening updater v0.1.2: DB nunca se sobreescribe + fallback de endpoints
- **Hallazgo CRITICO (verificado en vivo):** el instalador NSIS PISABA la DB del usuario. tauri-bundler template usa `File /a "/oname=registro.db"` (SetOverwrite on default; installer.nsi:655). Reinstalar el setup 0.1.2 sobre una instalación con datos QA -> hash de registro.db cambió y servicios=0. El claim previo de AGENTS ("solo escribe si no existe") era FALSO; el flujo del updater (corre el mismo setup) habría borrado todo el histórico de la tienda.
- **Fix:** la DB ya no viaja como recurso `registro.db` sino como plantilla **`registro.default.db`** (`bundle.resources: {"../registro.db": "registro.default.db"}`). El seed ocurre en `get_db_path()` (lib.rs, caso 1b + caso 3 APPDATA): copia la plantilla SOLO si `registro.db` no existe (primer arranque). Verificado: reinstalación sobre datos -> hash idéntico; DB borrada -> recreada desde plantilla (980 productos).
- **Multi-endpoint verificado:** `plugins.updater.endpoints` es un ARRAY; el plugin (tauri-plugin-updater core.rs ~L483) prueba en orden y solo rompe el loop con 2XX + JSON válido (error/timeout/status no-2XX -> siguiente). E2E en vivo: endpoint 1 roto (connection refused) -> endpoint 2 local (http.server 8901) -> dialog "0.1.3" -> kit -> instalación -> relanzamiento -> health check ok -> estado ok. **DB con datos QA intacta** (1 servicio + 1 cliente + 1 día + 980 productos).
- **release.ps1:** soporte opcional Google Drive (`-DriveLatestId/-DriveSetupId` + `tools/drive_ids.json` -> `latest_drive.json` con `https://drive.usercontent.google.com/download?id=<SETUP_ID>&export=download`; regla: sobrescribir el mismo archivo en cada release para que los IDs sean estables).
- **Build FINAL 0.1.2 firmado** (setup 5.975.222 B + .sig): endpoints revertidos a GitHub único (sin localhost ni dangerousInsecureTransportProtocol). Instalado sobre la instalación QA: hash DB idéntico, 980 productos, DEV-TEST-UPD visible, día abierto 748.79. Datos QA limpiados después (0 services/clients/días).
- Artefactos: `instaladores\Registro Servicio Tecnico_0.1.2_x64-setup.exe` + `latest.json` (URL GitHub `releases/download/v0.1.2/` + firma real) + `Registro.exe` raíz + `registro.db` -> target/release.
- Observed (menor): el useEffect de App.tsx que corre `mark_update_ok` tras el PIN no siempre completaba el flujo en el primer intento (quedó pending; marqué ok manualmente via CDP). Inofensivo: el siguiente arranque repite health check y marca ok; watchdog expira a los 90s.
- GitHub volvió a responder (200/404 <0.5s, intermitente) -> ESTADO.md P1-1 actualizado; release v0.1.2 sigue pendiente de `gh auth login` (token 401). Drive NO configurado (usuario no proporcionó IDs) -> P1-5 como paso manual documentado.
- Checks: cargo test 26/26 (2x), npm run build OK, Entropy Registry +2 filas (NSIS overwrite, GitHub intermitente), feature_list #22, ESTADO.md QA actualizado.

## 2026-08-06 - UI fix: cards de servicios + dialog de abono descuadrados
- **Card de servicio:** la fila inferior `flex justify-between` aplastaba los badges de trabajos (6 botones de acción shrink-0 ≈370px en cards de ~540px) -> los trabajos quedaban uno encima del otro y los botones desbordaban. Fix: badges en fila propia full-width (`flex-wrap` + `whitespace-nowrap`) y botones al pie (`flex flex-wrap gap-1.5 border-t pt-3`) — nunca se salen.
- **PaymentDialog:** `sm:max-w-sm` (384px) desbordaba: footer de 3 botones (~370px) fuera del contenedor, tabla de pagos apretada, sin max-h (footer fuera de pantalla en ventanas bajas). Fix: `sm:max-w-md` + patrón Libro Diario (`max-h-[88vh] flex flex-col overflow-hidden`, header/footer `shrink-0`, cuerpo `min-h-0 flex-1 overflow-y-auto`, tabla `overflow-x-auto`, footer `flex-wrap`).
- **Pills "Trabajos / Fallas"** del form: `whitespace-nowrap` (antes "Software / Formateo" se partía en 2 líneas, alturas disparejas).
- Verificación: npm build OK (25s), cargo test 26/26, harness loop GOAL MET (31.8s, 0 errores), release rebuilt (Compiling registro, 7m21s), Registro.exe == target/release (hash 73E98914...), app relanzada con el fix.

## 2026-08-06 - Modal de historial de cliente moderno + ServiceForm con footer fijo
- **Clients.tsx modal modernizado** (skill shadcn + patrones del harness): header con chip de avatar (User en rounded-lg bg-primary/10) + Badges de cédula (FileText), teléfono (Phone) y última actividad (CalendarDays); KPIs compactos con icono en chip de color (patrón Libro Diario): Total Gastado (Wallet/emerald), Servicios (Wrench/primary), Compras (ShoppingCart/amber), **Por cobrar** (CircleDollarSign/danger, calculado de clientServices); tablas de servicios (9 col) y compras envueltas en `rounded-md border overflow-x-auto` (nada fuera de rango); headers de sección con chip + contador Badge; badge "Por cobrar $X" al lado del título de Servicios; empty state con icono en border dashed; ServiceDetail reescrito con composición completa Card (CardHeader/CardTitle/CardContent) en vez de divs planos; badges de estado con color (Entregado verde / Por entregar ámbar / Cancelado-Devuelto rojo).
- **ServiceForm**: patrón de dialog del Libro Diario — `max-h-[88vh] flex flex-col overflow-hidden`, DialogHeader `shrink-0 pr-6`, banners de garantía `shrink-0`, cuerpo `min-h-0 flex-1 overflow-y-auto pr-1`, DialogFooter `shrink-0 border-t pt-3` → Cancelar/Guardar SIEMPRE visibles (antes se escrollaban fuera = "fuera de rango").
- **Cards de servicios**: botones de acción con `flex-1` (Entregar/Pago/Factura/Editar) → distribución uniforme al hacer wrap, nada desalineado.
- Verificación: npm build OK, harness loop GOAL MET (33.7s, 0 errores), release rebuilt (6m15s) + Registro.exe == target/release (hash A07E480B...) + app relanzada (PID 11004).

## 2026-08-06 - Pedidos: revisión completa + hardening funcional (E2E verificado)
- **Revisión de todo el flujo** (backend db.rs + commands.rs + frontend Pedidos.tsx): schema purchase_orders/items correcto (order_date default), add/get/get_items/mark_received/delete/reorder_suggestions cableados, types alineados.
- **E2E en sandbox (CDP, app real de producción):** open_day → get_reorder_suggestions (8 productos, incluye stock negativo -1) → add_purchase_order (2 items, 5 unidades, $9) → get_purchase_orders (Pendiente) → get_items (2) → mark_received (**stock 0→3, delta exacto**) → doble receive = error correcto "Este pedido ya fue recibido." → estado Recibido → delete (desaparece). **TODO FUNCIONAL**.
- **Hardening UI (Pedidos.tsx):** "Recibido" con **AlertDialog de confirmación** (conteo de artículos/unidades, evita sumar stock por click accidental); receive/remove/openDetail con try/catch + **banner de error** en la página (antes errores silenciosos); dialog Nuevo Pedido con **banner de día cerrado** (ácaro: `require_open_day` solo fallaba al guardar sin avisar) + Guardar deshabilitado; dialogs Nuevo/Detalle con patrón del Libro Diario (footer siempre visible); recibir desde el detalle refresca items/estado en vivo.
- Verificación: npm build OK, harness loop GOAL MET (36.5s, 0 errores), release rebuilt (8m50s) + Registro.exe actualizado + app relanzada (PID 12240). Sandbox E2E eliminado.

## 2026-08-07 - v0.1.4: "Ver más tarde" descarga en segundo plano + aviso al reiniciar (E2E verificado)
- **Feature:** botón "Ver más tarde" (antes "Recordar después") en UpdateDialog → `update.download()` en segundo plano con progreso, guarda `localStorage('update_downloaded_v')` y cierra sin instalar. Al reiniciar, el check de arranque muestra el dialog de nuevo con banner verde "Ya descargaste esta actualización — está lista para instalar". Dismiss forever eliminado (`clearDownloaded` al instalar OK). App.tsx ya no consulta `dismissedVersion`.
- **Decisión de repo:** el usuario pidió GitHub privado pero GitHub NO sirve assets de releases privadas sin auth (el updater no lleva token) → repo principal sigue PUBLICO; se creó y borró `Service_Tecnico-Releases` (descartado). Endpoint del updater en `releases/latest/download/latest.json` del repo principal; fallback documentado: Google Drive (multi-endpoint).
- **E2E en vivo (endpoint local + build debug):** app 0.1.4 con endpoint local → detecta "0.1.5" (setup 0.1.4 firmado renombrado) → dialog "Ver más tarde" → click → descarga en 6s → `update_downloaded_v=0.1.5` → dialog cerrado sin instalar → reiniciar → dialog visible + nota "ya descargaste esta actualización". Kit de rescate (backup + prev + watchdog) generado. Instalación NSIS en %LOCALAPPDATA% (destino por defecto — flujo completo ya verificado contra GitHub real en la iteración 0.1.3).
- Verificación: npm build OK, cargo 26/26, harness loop GOAL MET (65.7s, 0 errores), release v0.1.4 publicada en GitHub (URL normalizada con puntos), endpoint 200 + bytes idénticos (hash 472207E4...), commit fee012c pusheado.

## 2026-08-08 - Día limpio: Title Case, CRUD Clientes, Vista por Día, modo dev con DB aislada
- **Title Case:** `title_case` (db.rs) + `titleCase` (utils.ts) normalizan nombres en TODOS los write points (add_client, add_or_find_client, insert_service_row/update_service, add_sale, save_client) + migración idempotente `migrate_title_case_names` (clients/services.client/sales.client_name, sin duplicar) + `onBlur` en ServiceForm/SaleForm.
- **CRUD Clientes:** comando `save_client` (upsert por cédula con variantes V-/E-/dígitos → nombre exacto; UPDATE propaga rename a snapshots services.client/sales.client_name); `get_clients` con address/email/notes (apendados) y suma por client_id con fallback por nombre; Clients.tsx con botón "Nuevo cliente" + Pencil por fila → ClientFormDialog.
- **Vista por día:** Ventas abre en "Hoy"; Servicios con sentinel `__activos__` (status NOT IN finales) + botones Hoy/7 días/30 días/Todo; Libro Diario con tarjeta "Resumen del día" (`get_day_summary`: recibidos/entregados/en taller/pagos/ventas por moneda); Dashboard KPI "Cobrado Servicios Hoy" (misma fórmula del Libro Diario) + subtítulo "Facturado histórico $X (N entregados)" y "Equipos en Taller" excluye Entregado; Help.tsx sección "El día de trabajo".
- **Modo dev aislado (REGISTRO_DB):** override en `get_db_path()` (lib.rs) → la app dev usa `dev_registro.db` sin tocar la real. `tools/seed_dev_db.mjs`: copia registro.db → dev_registro.db (checkpoint WAL), asegura columnas faltantes (PRAGMA table_info + ALTER idempotente; lección: `${c}` interpolado en el ALTER o la columna se llama "REAL"), borra datos de negocio, siembra 4 clientes en minúsculas + 6 servicios (estados mezclados + orden multi-equipo DEV-0003/DEV-0003-A) + 4 abonos (incl. Bs 18720 Pago Móvil) + 3 ventas + cierre de ayer + día abierto hoy (tasa 748.79). `tools/verify_dev_db.mjs` + `verify_dev_db2.mjs` verifican el estado.
- **Verificación en vivo:** `$env:REGISTRO_DB="...dev_registro.db"; npx tauri dev` → app (PID 5696) corriendo contra dev_registro.db; migración title_case aplicada (clientes "Maria Fernandez", "Roberth Silva"); abonos Bs→USD OK (18720/748.79 = $25.0003); el usuario probó la app en vivo (pagos a DEV-0001, abonos Pago Móvil/Punto Bs, entrega de DEV-0004, orden nueva DEV-0006 $12.5).
- Verificación: cargo test 34/34 (5 tests nuevos: title_case helper/write+migration, save_client rename, get_day_summary, sentinel activos), npm run build OK (9.13s), governance --build-only PASS, AGENTS.md actualizado (74 comandos + sección "Nombres en Título, CRUD de Clientes y Vista por Día" + 4 filas Entropy Registry). Sin release (el usuario pidió terminar el ciclo dev).

## 2026-08-10 - Impresora Bluetooth (MP58-04BLE), fix crítico de totales, Help bento y release v0.1.6
- **Fix CRÍTICO dialog Cerrar Día en $0.00 (InvalidColumnType):** `tools/seed_dev_db.mjs` insertaba la venta de Pago Móvil con 11 args para 12 placeholders (faltaba `''` de notes) → `net_amount` quedó con TEXT `'REF-VENTA-01'` (zelle_reference='VES'). node:sqlite NO lanza error con args faltantes (verificado: `.run(1,'x','y')` para 4 placeholders → 4º NULL). `compute_daily_totals` (db.rs) lee net_amount como índice 4 → `Invalid column type Text at index: 4, name: net_amount` → el dialog mostraba todo en $0.00 (no hacía falta cerrar el día: los totales se calculan con día abierto). Fix: `CAST(COALESCE(net_amount,total) AS REAL)` en las 3 ramas UNION (texto → 0.0, nunca crash) + fila saneada en dev_registro.db (`notes=NULL, net_amount=7487.9, zelle_reference='REF-VENTA-01'`) + seed corregido con `''`. Test `test_daily_totals_resilient_to_text_net_amount` (primera aserción falló: los buckets usan `total` REAL sano, no `net` — corregida con usd_cash_total=5.0, pago_movil_total=7487.9, grand_bs=7487.9, grand_total≈5+7487.9/40.5).
- **Test date-dependent:** `test_get_day_summary` usaba `let today = "2026-08-07"` hardcodeado → con el reloj en 10-08 los servicios creados con `date_in=now()` no contaban como "recibidos hoy" → fallaba (lección: NUNCA hardcodear fechas en tests que crean filas con datetime('now')). Fix: `chrono::Local::now().format("%Y-%m-%d")` + `&today` en get_day_summary.
- **Punto de Venta ($)/(Bs) separados (DailyLedger.tsx):** "Cobros del día por método" con UNA fila "Punto de Venta ($ + Bs)" → DOS filas condicionales (`pos_net_usd>0.005` → "Punto de Venta ($)" text-success; `pos_net_bs>0.005` → "Punto de Venta (Bs)" text-warning, ambas con "neto tras comisión"); párrafo "El sistema cobró ... por Punto" dinámico por moneda o "No hubo cobros por Punto de Venta hoy.". Los inputs "Monto impreso ($)/(Bs.)" ya estaban separados.
- **Impresora Bluetooth (printer.rs reescrito):** `bluetooth_friendly_names()` lee el registro de Windows vía `reg.exe query HKLM\SYSTEM\CurrentControlSet\Enum\BTHENUM /s /v PortName` (patrón curl.exe, sin deps) → mapea COMx → reverse-MAC; `bluetooth_device_name(addr)` resuelve el nombre amigable en `HKLM\...\Services\BTHPORT\Parameters\Devices\<ADDR>` (`Name REG_SZ`, ej. "MP58-04BLE"). `list_com_ports` ahora muestra `Bluetooth · <nombre>` (fallback "Bluetooth"); `print_receipt` da error específico para puertos BT ("verifica que la impresora esté pareada (Configuración → Bluetooth → Más opciones → Puertos COM)"). 4 tests nuevos (parse_bthenum_output, ignores_no_com, mac_from_device_path, never_panics — 7 total en printer). La MP58-04BLE aún NO está pareada en la PC (reg BTHENUM vacío): el usuario debe parearla en Windows (Bluetooth y dispositivos) o conectarla por USB (el puerto COM aparece solo tras parear/enchufar).
- **Help.tsx bento grid:** 6 cajas de acción → grid `md:grid-cols-2 lg:grid-cols-4` con "Abrir el día" y "Ver el negocio" en `col-span-2` + gradiente de fondo; cada caja con barra de color superior (gradiente), chip de icono con ring (`bg-{color}-500/15 ring-inset`), hover elevación + glow del color (`hover:shadow-{color}-500/10`); **cajas clickeables** → `openGuide(target)` abre la sección del accordion (controlado con `value/onValueChange`) + scroll suave al Card de la guía (ref + scrollIntoView, keyboard Enter/Espacio). Header: "Elige qué quieres hacer — te mostramos el paso a paso".
- **Lanzamiento de la app dev (lección de procesos):** `Start-Process npx.cmd -RedirectStandardOutput` hace que el tool del shell se cuelgue esperando el pipe del hijo (el watcher retiene los handles) → timeout → **tree-kill mata el watcher completo**. La app "desaparecía" porque el timeout del shell mataba el árbol (Start-Process children incluidos). También: `npx` desde `cmd /c` con `cd /d C:\Users\ROBER` (PERFIL, no el proyecto) no resolvía `tauri` local → fetch del paquete `tauri` del registry → "could not determine executable to run" (el CLI real vive en `C:\Users\ROBER\registro\node_modules\.bin\tauri.cmd`). **Método que funciona: lanzar detached vía WMI** — `Invoke-CimMethod Win32_Process.Create` con `cmd /c "set REGISTRO_DB=...&& cd /d C:\Users\ROBER\registro&& node_modules\.bin\tauri.cmd dev >> log 2>&1"` (proceso fuera del árbol del shell, sobrevive timeouts, log a archivo).
- **Plantilla del instalador saneada:** la DB raíz `registro.db` (fuente de `registro.default.db`) tenía 1 cierre de prueba → borrado (`backup/registro_template_20260811.db` guardado). Verificado: 980 productos, 0 ventas/servicios/cierres/clientes/pedidos, PIN 1234, sin día abierto. La actualización NO toca DBs: cada PC usa su propia registro.db (offline-first), el seed de plantilla solo corre si la DB no existe (lib.rs get_db_path), el updater respalda (updates/registro.backup_pre_vX.db + prev/registro.exe) y hace rollback automático si el health check falla.
- Verificación: cargo test 39/39 + doctests OK (4 tests printer nuevos + resilience + fecha dinámica; lección doctest: doc-comment con ``` sin `text` se ejecuta como doctest y falla), npm run build OK (4.2s), governance --build-only PASS. Release v0.1.6 publicada en GitHub (repo público shaman2527/Service_Tecnico), setup firmado + latest.json, instaladores/ actualizado.

## 2026-09-16 - F3 CERRADA: pestaña «Modelos» (padrón de teléfonos) + revisión adversarial
- **Feature 23 cerrada (done)** — pestaña **Modelos** en Inventario (tabla `phones`, copia de trabajo con 1154 teléfonos / 217 «por revisar»): **Marca como columna y filtro** (Select con conteos por marca), **orden de 3 estados** (`sin orden → ascendente → descendente`) en Teléfono/Marca/Repuestos/Stock/Estado con `aria-sort`, búsqueda por tokens, vistas Con repuestos / Con stock / **Por revisar** + botón «Ver los 217 por revisar», paginación server-side (50/pág), KPIs y **ficha de solo lectura** con los repuestos por categoría (Pantalla primero). La pestaña de consulta se renombró «Por modelo» → **«Repuesto por modelo»** (Help.tsx y AGENTS.md actualizados).
- **Archivos:** `src/components/inventory/ModelsTab.tsx`, `PhoneDetailDialog.tsx`, `Kpi.tsx` (franja compartida con Productos), `src/lib/phoneOrder.ts` (lógica PURA del orden de 3 estados), `tools/verify_models_tab.mjs`, `tools/progress/specs/F3-modelos-tabla.md`; tocados `Inventory.tsx`, `ProductsTab.tsx`, `Help.tsx`, `types.ts`, `db.ts`, `src-tauri/src/phones.rs`, `commands.rs`, `vite.config.ts` (ignora los temporales de los editores: sin eso, editar un .tsx mataba Vite con EBUSY y con él `tauri dev`), `tools/cdp_driver.mjs` (`awaitPromise`).
- **Backend:** `get_phones` gana `only_review`; `dir` manda en **todas** las columnas (antes 3, con un `reverse()` que invertía el desempate) usando claves precalculadas con `catalog::norm` y desempate por nombre + id; `PhoneBrandRow` gana `needs_review`; **`merged_stats` reescrito** (unión clave+alias = única fuente de repuestos/stock/categorías) — eso arregla que la **ficha quedara vacía tras renombrar** (bloqueante detectado en la revisión adversarial) y el stock de la lista (daba 0). `limit.clamp(0,1000)` + `saturating_add`. **Ningún comando de escritura nuevo.**
- **Verificación:** `cargo test` **84/84** (tests nuevos: orden de 3 estados + filtro por revisar, ficha/índice que sobreviven al renombrado, sin doble conteo por alias); `npm run build` ✓; `npm run lint` **0 errores** (sin warnings nuevos); **CDP en vivo 23/23** + sanidad Productos/enlace 3/3; **contraste SQL↔comandos 12/12** (1154 / 217 / 26 marcas / Samsung 261 / órdenes asc-desc); `harness_security` PASS; `harness_truth` PASS; `npx tsx tools/cli/index.ts parallel` (security+review+build) ✅; **revisión adversarial con subagente** (BLOCK inicial: 1 bloqueante + 10 hallazgos) y **re-verificación independiente de los fixes (FIXES OK)**.
- **Hallazgo nuevo registrado (feature 27, autorizado por el usuario como siguiente):** los 6 `SELECT p.*, c.name` de `db.rs` mapean `category_name: r.get(14)` pero `search_text` quedó en cid 14 por un ALTER TABLE → la columna «Categoría» de Productos muestra el texto de búsqueda (verificado por IPC en vivo). Corregido en `phones.rs` con lista explícita de columnas.
- **Decisiones del usuario (2026-09-16) para la feature 24:** (1) **Poco es línea propia** → nombre «Poco X3» y la clave conserva `poco` (hoy «Poco X3» da `xiaomi|x3` y «Redmi Poco X3` da `xiaomi|poco x3`), normalizando los 22 «Mi Poco …» y los 6 «Redmi Poco …»; (2) **Honor y Realme mandan** sobre la marca madre («Huawei Honor X6A» → «Honor X6A», «Oppo Realme C35» → «Realme C35») y los pares se fusionan conservando los repuestos de ambos (≈14 pares Honor/Huawei + Realme/Oppo + `ZTE Blade L8`/`ZTE L8` + `Alcatel 5001 1V 2019`/`1V 2019`); (3) prioridad: **primero el bug de la columna Categoría (feature 27) y después renombrar los 217** (feature 24).
- **Nota de herramienta:** `harness_review` no corre en esta copia (espera `tools/reviewer/parallel-review.ts`); se reemplazó por el CLI del proyecto + subagentes revisores. `harness_learn` **reescribe** `tools/progress/patterns.md` (se recuperó con `git checkout --`; el volcado quedó en `patterns.md.harnesslearn.bak`). `harness_close` marcó «deploy readiness: FAIL» — esperado y justificado: **modo DEV sin release ni push** (instrucción del usuario) y app de escritorio sin destino de despliegue; tests/build/truth pasaron.
- **Sigue todo en MODO DEV**: sin release ni push.

## 2026-09-16 - F27 CERRADA: la columna «Categoría» del inventario mostraba el texto de búsqueda
- **Feature 27 cerrada (done)** — bug pre-existente detectado por la revisión adversarial de F3: `products` tiene **15 columnas físicas** y `search_text` quedó en **cid 14** por un `ALTER TABLE`; los **6** `SELECT p.*, c.name as category_name` de `db.rs` mapeaban `category_name: r.get(14)` → leían `search_text`. Efecto visible: **Inventario → Productos → columna «Categoría»** mostraba «pantalla infinix hot 30i go 2023 …» en vez de «Pantalla» (verificado por IPC en vivo antes del fix).
- **Fix:** constante única **`PRODUCT_COLS`** con lista **EXPLÍCITA** de columnas (`0..13` producto, **14 = `c.name`**) aplicada en los 6 sitios: `get_products_page`, `find_compatible_products` (selector de pantalla del servicio), `get_products`, `get_low_stock_products`, `get_reorder_suggestions`, `suggest_products`. Con la lista explícita los mapeos posicionales existentes quedan correctos sin tocarlos. Cero `SELECT p.*` en el backend (regla del proyecto: las migraciones son `ALTER TABLE`).
- **Test nuevo:** `db::tests::test_product_category_name_is_real_category` — recorre los 6 comandos y compara `category_name` contra la tabla `categories` (y `price_usd`, que sigue leyéndose en 13). Antes del fix fallaba con «category_name = Some("Teléfono")/texto de búsqueda».
- **Verificación:** `cargo test` **85/85**; `npm run build` ✓; `npm run lint` 0 errores; **en vivo** `get_products` / `get_products_page` / `suggest_products` / `get_low_stock_products` → `category_name: "Pantalla"` y la **columna «Categoría» de la UI = «Pantalla»** (5 filas revisadas por CDP); **sin regresión** en Modelos (23/23) ni en el contraste SQL↔comandos (12/12); KPIs idénticos a SQL (1083 SKU · 1 con stock · 6 u. · 26 marcas); `harness_security` PASS, `harness_truth` PASS y `tools/cli parallel` (security+review+build) ✅.
- **Sigue todo en MODO DEV**: sin release ni push. Próximo: **feature 24** (renombrar los 217 + fusionar con las reglas Poco/Honor/Realme ya aprobadas por el usuario).

## 2026-09-16 - F24 CERRADA: renombrar y fusionar la lista de teléfonos (con las reglas Poco/Honor/Realme)
- **Feature 24 cerrada (done)** — la lista maestra del taller ya se corrige DESDE la app (Inventario → Modelos, **solo el dueño**): «**Corregir**» (marca + línea + modelo con **vista previa** del nombre comercial, aviso si ya existe otro teléfono con ese nombre + oferta de fusionar y cuántos repuestos conserva), «**juntar**» (fusiona dos fichas del mismo teléfono escrito distinto: la que abrís se queda y hereda los alias) y «**Agregar teléfono**».
- **Reglas canónicas aprobadas por el usuario:** «POCO» es línea propia → nombre «Poco X3» (sin «Mi» ni «Redmi» delante) y la **clave conserva `poco`** (como el `iPhone` en Apple), así «Poco X3» y «Redmi Poco X3» son el MISMO teléfono; «HONOR» y «REALME» mandan sobre la marca madre (`familyBrands`: honor→Honor, realme→Realme) → «Huawei Honor X6A» = «Honor X6A», «Oppo Realme C35» = «Realme C35».
- **Resultado sobre la copia de trabajo:** 1154 → **1135** teléfonos y 217 → **162** «por revisar»; tras fusionar «8P» con «Spark 8P» desde la app: **1134 / 161**. Claves viejas eliminadas: `huawei|honor*`, `oppo|realme*`, `honor|honor*`, `xiaomi|x3`. **Ningún producto, stock ni precio fue tocado** (solo la tabla `phones`). Respaldos: `backup/pre_f24_reglas_20260916.db` (copia consistente) + `backup/backup/registro_pre_telefonos_20260916_004340.db` (del hook).
- **Backend:** `phones.rs::resolve_naming` (UNA sola forma de calcular nombre/modelo/clave, la misma que el padrón) → `rename_phone` ya no deja el nombre viejo duplicado en la próxima reconstrucción (`rebuild_phones`); `rebuild_phones` **no resucita** las claves reclamadas por los alias de las filas `source='manual'`; `merge_phones` guarda además la marca+modelo del borrado como alias; comandos nuevos **`preview_rename_phone`** (solo lectura) y **`can_edit_phones`**; **gate de escritura real** con `Database::owner_unlocked` (lo activa `verify_pin` con el PIN correcto; sin PIN configurado se permite porque es un solo usuario); `get_phone_models` (lista del formulario de servicio) lee el **padrón** y `find_compatible_products` **resuelve los alias** (renombrar no le hace perder la pantalla compatible).
- **Frontend:** `src/components/inventory/PhoneEditDialog.tsx` (editar/alta con vista previa + diálogo de fusión), `ModelsTab.tsx` (prop `canEdit`, botones «Corregir», «juntar», «Agregar teléfono», aviso en la vista «Por revisar»), `Inventory.tsx` (`canEdit={role === 'owner'}`), `types.ts` (`PhoneListRow.key`, `RenamePreview`), `db.ts` (4 llamadas nuevas).
- **Verificación:** `cargo test` **89/89** (tests nuevos: reglas Poco/Honor/Realme, `preview_rename_phone`, renombrar sin duplicado tras reconstruir, gate de dueño) · `npm run build` ✓ · `npm run lint` **0 errores** (sin warnings nuevos) · **CDP 23/23** (F3 sin regresión, 1134/161) + **9/9** (dueño: KPIs UI==backend, fichas «Poco X3»/«Honor X6A»/«Realme C35», vista previa y cancelar) + **6/6** (cajera: no ve los botones y el **backend rechaza** el invoke con «Solo el dueño puede cambiar la lista de modelos…») + **escrituras reales** con `tools/verify_phones_write.mjs` (fusión «8P»→«Spark 8P» con −1 teléfono, alta «Prueba F24», renombrado a «Test F24»; la ficha de prueba se borró después) · `harness_security` PASS · `harness_truth` PASS · `tools/cli parallel` ✅.
- **Scripts nuevos:** `tools/verify_phones_edit.mjs` (dueño y `--cashier`), `tools/verify_phones_write.mjs`. Spec: `tools/progress/specs/F24-renombrar-fusionar.md`.
- **Pendiente dentro de esta línea:** los 161 teléfonos que siguen «por revisar» no se deducen de una regla (p. ej. «11T Pro», «8T»): se corrigen a mano con «Corregir».
- **Sigue todo en MODO DEV**: sin release ni push.

### F24 — revisión adversarial y cierre (misma jornada)

- **Revisión adversarial independiente:** veredicto inicial **BLOCK** con **1 bloqueante + 2 mayores + 7 menores**, todos corregidos:
  1. **BLOQUEANTE:** si el renombrado **cambia la marca**, la clave vieja no quedaba reclamada → el rebuild recreaba la ficha vieja y la renombrada quedaba **sin repuestos**. Fix: `rename_phone` guarda el **nombre y marca+modelo viejos como alias** (test `test_rename_con_cambio_de_marca_no_recrea_la_ficha_vieja`).
  2. El rebuild **pisaba** las filas `source='manual'` (incluidos sus alias = vínculo con los repuestos) → ahora las omite (solo refresca `needs_review`) + test `test_rebuild_no_pisa_las_filas_manuales`.
  3. `rename_phone`/`merge_phones` no validaban que las filas existieran (el merge podía borrar una ficha sin guardar sus alias) → error claro + test `test_rename_y_merge_validan_que_existan`.
  4. Menores: vista previa con choque mal etiquetada («Fusionados quedarían N repuestos»), `owner_can_edit` **fail-closed**, la UI ahora usa `can_edit_phones`, `get_phone_models` ya no esconde las altas manuales (y busca por marca + nombre), **`<Toaster />` montado en `App.tsx`** (los avisos de TODA la app no se veían), icono/aria del botón de fusionar, comentario doc partido.
- **Antes de la revisión ya había corregido el mismo defecto por la vía de la FUSIÓN** (encontrado al comprobar la idempotencia): fusionar dos fichas del catálogo no se sostenía porque la sobreviviente quedaba como `catalogo`; ahora `merge_phones` la marca `manual` (rebuild en seco: **creados 0 · sin cambios 1134**).
- **Estado final:** `cargo test` **93/93** · build ✓ · lint 0 errores · CDP **23/23** (F3) + **9/9** (dueño) + **6/6** (cajera) · `harness_security`/`harness_truth` PASS · CLI parallel ✅ · **feature 24 cerrada (done)**.

## 2026-09-16 - F25 + F26 CERRADAS: cargar el inventario desde la app y regla «solo Pantalla»
- **Feature 25 (done) — Asistente de carga de inventario.** Inventario → Ajustes → «Cargar la lista del local»: se pega o se abre el `.txt` tal como lo tiene el local (una marca por línea, debajo sus modelos; `modelo (N)` = unidades; `A30/A50` = la misma pantalla para los dos teléfonos), la app lo **parsea** y lo **cruza** contra el catálogo — **solo pantallas**, con **gate de marca** y calidad exacta/prefijo/parecida, hasta 6 alternativas por línea —, muestra una **vista previa editable** (cantidad por fila + producto elegido; lo que no encuentra **lo avisa**, nunca inventa una ficha) y al **aplicar** hace **RESPALDO** (`backup/registro_pre_carga_<fecha>.db`), deja el stock por producto y anota el **movimiento** «Carga de inventario» (con la opción, marcada por defecto, de dejar en 0 las pantallas que no están en la lista). No toca precios ni compatibilidad y **exige el PIN del dueño**.
  - Módulo nuevo `src-tauri/src/loadlist.rs` (parseo + cruce + aplicar), comandos **`preview_inventory_load`** y **`apply_inventory_load`**, UI nueva `src/components/inventory/LoadInventoryDialog.tsx` (3 pasos, con «Abrir un archivo .txt» vía los plugins dialog/fs que ya estaban habilitados), tarjeta en `PricesTab.tsx`, tipos y puente en `types.ts`/`db.ts`.
  - **Defensivo (revisión propia antes del cierre):** ignora fichas de **otra categoría**, ids inexistentes y líneas **repetidas** del mismo producto (cuenta `skipped`), **acota** las cantidades a 100.000 y respalda **antes** de escribir. 4 tests (`parse_list`, `preview_load` con gate de marca, `apply_load` con respaldo/movimientos/idempotencia, y el caso de fichas inválidas).
- **Feature 26 (done) — Regla «solo Pantalla».** `catalog::PHONE_CATEGORY = 1` se usa en `rebuild_phones` (qué teléfonos existen) y en `phones::phone_index` (qué repuestos/stock cuenta cada teléfono): el padrón y sus números hablan **solo de pantallas**. La pestaña **Productos** abre con el filtro de categoría en **Pantalla** (buscando la categoría por NOMBRE, no por id) y el filtro se puede quitar para ver el resto del catálogo.
  - Datos sobre la copia de trabajo: padrón **1134 → 1079** teléfonos y «por revisar» **161 → 142** (25 marcas), con los **1083 productos y 6 unidades intactos** (48 fichas quedan fuera del padrón por no ser pantallas). Respaldos: `backup/pre_f26_solo_pantalla_20260916.db` + `backup/backup/registro_pre_telefonos_20260916_074531.db`.
- **Verificación:** `cargo test` **97/97** · `npm run build` ✓ · `npm run lint` 0 errores · **CDP 10/10** (`tools/verify_inventory_load.mjs`: el Inventario abre en «Pantalla», el asistente está en Ajustes, el paso 1 acepta lo pegado, el cruce muestra 3 líneas / 2 cruzadas / 1 sin producto con aviso, cantidad editable por fila, cerrar sin aplicar NO toca el stock y aplicar hace respaldo) · regresión **23/23** en Modelos (1079/142) y 9/9 + 6/6 en la edición del padrón · `harness_security`/`harness_truth` PASS · `tools/cli parallel` ✅.
- **Herramientas de verificación mejoradas:** `tools/cdp_driver.mjs` ahora hace `scrollIntoView` antes de cada clic (un botón fuera de pantalla no recibía el clic) y expone `insertText` para pegar texto multilínea (los `\n` no viajan como teclas). Script nuevo `tools/verify_inventory_load.mjs`.
- **Spec:** `tools/progress/specs/F25-F26-carga-y-solo-pantalla.md`. **Con esto el backlog del módulo de inventario queda sin features abiertas** (1-27 cerradas); sigue todo en MODO DEV (sin release ni push).

### F25/F26 — revisión adversarial y cierre (misma jornada)

- **Revisión adversarial independiente (2ª vuelta, con evidencia):** veredicto **BLOCK**; todos los hallazgos corregidos y verificados con test o en vivo:
  1. **BLOQUEANTE — se perdían unidades reales:** dos líneas que describen la MISMA pantalla (`13C (6)` + `Redmi 13C (12)`) dejaban solo la primera → ahora **las unidades se suman por ficha** (`sum_qty` en la vista previa + `per_product` al aplicar). En la lista real del local eran **54 unidades** las que se perdían.
  2. **BLOQUEANTE — el paso 3 (resumen) nunca se veía:** al aplicar, `onChanged` refrescaba **y** saltaba a la pestaña Productos → React desmontaba el diálogo. `PricesTab` ahora recibe **`onRefresh`** (refrescar sin desmontar) y el resumen queda visible con el respaldo y los movimientos.
  3. **MAYOR — el barrido lo elegía quien llamaba:** `apply_inventory_load` aceptaba `categoryId`; un invoke a mano con `48` (Batería) **vaciaba esa categoría entera**. Se eliminó el parámetro de los dos comandos: la categoría es siempre `catalog::PHONE_CATEGORIES`.
  4. **MAYOR — el cruce no matcheaba lo que debía:** los `targets` iban crudos («Samsung A30») a un `match_quality` que compara contra la forma normalizada (`a30`) → **0 coincidencias** sin error visible. Ahora se normalizan con `phone_model_norm` (test de cruce en verde).
  5. **MAYOR — basura demasiado amplia:** un modelo solo de números se descartaba aunque trajera unidades (`13 (25)` = 25 iPhone 13). Solo es basura si la cantidad es 0.
  6. **MAYOR — `c/m 4G/5G`:** el «c/m» (con marco) partía mal el modelo y el `5G` suelto daba falsos «exacta». Se quita «c/m», se colapsa el espacio y no se cruzan partes de conectividad; el encabezado de marca ahora exige que el texto **sea** una marca conocida («Note» ya no re-marca sección).
  7. **El barrido se avisa ANTES de aplicar** (fichas y unidades que quedarían en 0), el **faltante (stock negativo)** que vuelve a 0 se anota como **entrada**, la **cantidad ilegible** («A30 (dos)») no se aplica sola, el respaldo aborta si no puede cerrar el WAL y su nombre es único.
  8. **Menores de UI:** `aria-label` en las cantidades y en los filtros, aviso de que **los KPI son de todo el catálogo** cuando la tabla está filtrada, plural correcto («1 pantalla»), el botón dice **fichas** y unidades reales.
- **F26 ajustada:** el conjunto es `[1 Pantalla, 18 Táctil, 19 Táctil Tablet]` — con solo `1` se perdían **55 teléfonos** que únicamente tenían repuestos de Táctil/Táctil Tablet.
- **Herramientas:** `tools/cdp_driver.mjs` pasa a **25 s + un reintento** en `Runtime.evaluate` (el WebView2 tarda en contestar mientras arranca la app; con 10 s la verificación moría por timeout con la app sana) y `tools/verify_inventory_load.mjs` crece a **18/18** verificando por la UI el paso 3, el aviso del barrido, las etiquetas accesibles y que **no se cambia de pestaña**.
- **Estado final de la 1ª vuelta:** `cargo test` **101/101** · build ✓ · lint 0 errores · CDP **18/18** (carga) + **23/23** (Modelos, 1079/142) + **9/9/6/6** (edición del padrón) · `harness_security`/`harness_truth` PASS · CLI parallel ✅ · features 25 y 26 cerradas (done).

### F25/F26 — 2ª vuelta de revisión adversarial, MEDIDA sobre la lista real del local (misma jornada)

La primera vuelta se hizo leyendo código; esta segunda midió **`tools/inventario_real.txt` (261 líneas / 713 unidades) contra una copia consistente de la base**, ejecutando `preview_load`/`apply_load` reales solo sobre copias. Encontró **2 bloqueantes + 6 mayores + 4 menores**, todos corregidos:

1. **BLOQUEANTE — el barrido dejaba en 0 fichas que la lista SÍ menciona.** Medido: de las 87 fichas que quedaban en 0, **79 (152 unidades) estaban escritas en la lista** (con un nombre que no cruzó: «RMA3 (10)» → *Pantalla Redmi RMA3* 10→0) y se anotaban movimientos de salida de mercancía que nunca salió. Fix: **GATE** — si la lista es todo y hay líneas con unidades sin pantalla asignada, **no se carga nada** y el error dice las tres salidas (asignar a mano, corregir el nombre en Productos, desmarcar el barrido) + **`keep_ids`** (el barrido no toca las fichas que la vista previa ya tenía) + el reporte informa `unassigned`/`unassigned_units`.
2. **BLOQUEANTE — el gate de marca comparaba la marca CRUDA del producto con la CANÓNICA de la sección.** La sección «Redmi» se canonicaliza a «Xiaomi», así que **57 fichas con `brand='Redmi'` (47 con stock, 119 unidades) quedaban fuera del cruce**: «RMA3 (10)» no veía *Pantalla Redmi RMA3* (stock 10). Fix: canonicalizar los dos lados (una ficha sin marca sigue siendo candidata).
3. **MAYOR — la contención por subcadena cruzaba modelos distintos:** «A33 c/m Bateria (1)» aterrizaba en la ficha del **A3** (2017) y le sumaba unidades. Fix: `catalog::contains_word` (contención por **palabra completa** — `norm()` conserva espacios) en `match_quality` y en el nombre de la ficha: «a3» ya no cruza con «a33» ni «15» con «redmi 15c».
4. **MAYOR — el texto de la línea nunca era objetivo del cruce:** «A17 c/m 4G/5G (1)» no podía caer en la ficha *Pantalla Samsung A17 c/m 4G/5G* (se llama igual) y el barrido se la llevaba a 0. Fix: el texto completo de la línea entra como target.
5. **MAYOR — el respaldo podía quedar VIEJO sin aviso:** `PRAGMA wal_checkpoint(TRUNCATE)` con `execute_batch` devuelve el flag `busy` como FILA (no como error) → un checkpoint ocupado pasaba desapercibido y la copia no tenía el WAL. Fix: **`VACUUM INTO`** (foto consistente que incluye el WAL) y, si falla, checkpoint leído + aborto con aviso si `busy != 0`.
6. **MAYOR — el tope de 100.000 existía solo al aplicar:** la vista previa y el botón prometían 999.999.999 u. y el reporte decía 100.000. Fix: tope en el **parseo** (vista previa = reporte) y la línea queda avisada para que la corrija el operario.
7. **MAYOR — 12 líneas / 31 unidades desaparecían en silencio** (sin candidatos y sin forma de asignarlas) y el resumen decía «0 líneas no se cargaron». Fix: `skipped` cuenta **líneas**, el reporte suma `unassigned`/`unassigned_units`, la vista previa avisa «N líneas sin pantalla (M u. que NO se cargan)» y el paso 3 lo repite. El buscador para asignarlas a mano queda como **feature 28 (pendiente)**.
8. **MAYOR — el aviso del barrido no se recalculaba al editar** (desmarcar una línea hacía que el barrido la dejara en 0 aunque el checkbox anunciara otra cosa): resuelto con `keep_ids` + el gate del punto 1.
9. **Menores corregidos:** `apply_inventory_load(rows=[], zero_missing=true)` ya no puede vaciar todo el inventario; candidatos por línea **6 → 15**; `catalog::contains_word` con tests; `aria-label` en cantidades y filtros; plural correcto; aviso de KPI vs filtro.
10. **Resultado MEDIDO (misma base, misma lista, antes → después):** cruzadas **249 → 260**, sin pantalla **12 → 1**, unidades aplicadas **682 → 712** de 713, fichas que el barrido dejaría en 0 **87 → 47**. La línea que queda sin pantalla es «6 c/m Accesorios (1)» porque en el catálogo se llama «Pantalla Redmi 6 c/m **Acasonor**» (error de tipeo del catálogo, no del cruce).
11. **Herramientas:** hook manual `test_manual_preview_real_list` (`$env:REGISTRO_PREVIEW_DB=<copia>` + `cargo test -- --ignored`) para medir el cruce real; `verify_models_tab.mjs` con la expectativa 1079/142 escrita; los scripts CDP **recargan la SPA** al empezar (antes se pisaban entre corridas) y `verify_phones_edit.mjs --cashier` queda documentado como «app recién arrancada» (el gate del backend vive en el proceso).
12. **Estado final:** `cargo test` **107/107** · build ✓ · lint 0 errores · CDP **20/20** (carga) + **23/23** (Modelos, 1079/142) + **9/9** (dueño) + **6/6** (cajera con app limpia) · `harness_security`/`harness_truth` PASS · CLI parallel ✅. Las features 25/26 siguen **done** (con las correcciones documentadas) y se abre la **28**.

## 2026-09-16 - F28 CERRADA: asignar a mano cualquier pantalla en el asistente de carga (cierra el módulo de inventario)

- **Feature 28 (done).** La lista escrita a mano y el catálogo no siempre se llaman igual: la línea **«6 c/m Accesorios (1)»** del conteo del local no cruzaba con la ficha **«Pantalla Xiaomi Redmi 6 C / Redmi M Acasonor / Redmi M Accesorios»** (`c/m` = **con marco**, aclarado por el usuario). Antes esa línea quedaba sin pantalla y, con el barrido marcado, **bloqueaba la carga** (a propósito: para no vaciar mercancía que sí está en la lista).
- **Ahora cada fila del asistente tiene buscador:** botón **«Buscar la pantalla»** (o «Buscar otra…» si la fila ya tenía candidatos) que abre un buscador por texto sobre las fichas de las **categorías de pantalla** (nombre + marca + modelo + compatibilidad: la misma búsqueda por tokens del inventario). Al elegir una ficha la fila queda **«asignada a mano»** y se comporta igual que un cruce automático: suma unidades por ficha, entra en `keepIds` (el barrido no la vacía) y deja de contar como `unassigned`.
- **Backend:** `loadlist::search_targets(conn, query, limit)` (solo lectura; `limit` acotado 1..50; exige 2+ caracteres porque una sola letra devolvía medio catálogo) + comando nuevo **`search_inventory_load_targets`** (registrado en `lib.rs`). Test `test_buscar_pantallas_a_mano` (busca por tokens en cualquier orden, no devuelve Batería, una letra → vacío, y la ficha asignada se aplica como cualquier otra).
- **Frontend:** `LoadInventoryDialog.tsx` (estados `manualIdx/manualQuery/manualHits/aMano`, `asignarAMano`, botón `data-load-hit` para poder automatizar la verificación) + `db.ts` (`searchInventoryLoadTargets`) + tipos.
- **Ayuda al usuario (Help.tsx, sección Inventario):** cómo contar la mercancía (marca por línea, `(N)` unidades, `A30/A50`, **`c/m` = con marco**), que se puede corregir a mano con el buscador, y el respaldo previo; y se documentaron los botones de escritura del padrón (F24) que faltaban.
- **Verificación en vivo con la LISTA REAL** (`tools/verify_inventory_load_real.mjs`, nuevo): 261 líneas / 713 unidades → 260 cruzadas + 1 sin pantalla (712 u.) → se busca «acasonor», se asigna → **261 cruzadas, las 713 unidades en 226 pantallas** y sin avisos pendientes; saliendo sin aplicar, el stock no se toca (**9/9**).
- **Regresión:** `verify_inventory_load.mjs` **27/27** (incluye los 6 chequeos nuevos de F28 y que la Ayuda lo explique) · `verify_models_tab.mjs` **23/23** · `verify_phones_edit.mjs` **9/9** (dueño) · `cargo test` **108/108** · build ✓ · lint 0 errores · `harness_security`/`harness_truth` PASS · CLI parallel ✅.
- **Lección de entorno (importante para futuras sesiones):** en esta PC la ventana de `tauri dev` carga **`http://tauri.localhost`** — sirve el **`dist` embebido en el binario**, NO el server de Vite: los cambios de frontend **no** se ven con HMR; hay que `npm run build` **y** recompilar/relanzar la app (un `cargo test`/reinicio basta) para que el binario embeba el `dist` nuevo. Comprobación rápida: comparar el nombre del chunk cargado (`performance.getEntriesByType('resource')`) con el archivo en `dist/assets/`.
- **Sigue todo en MODO DEV**: sin release ni push.

## 2026-09-16 - F28 + F29 CERRADAS: asignar a mano, carga rápida y proveedor (se cierra el módulo de inventario)

- **F28 (done) — asignar a mano cualquier pantalla.** La línea que el cruce no resuelve («6 c/m Accesorios (1)» contra «Pantalla Xiaomi Redmi 6 C / Redmi M Acasonor / Redmi M Accesorios»; `c/m` = **con marco**) se busca desde la fila y se asigna; la ficha elegida suma unidades, entra en `keepIds` y deja de contar como `unassigned`. Backend: `loadlist::search_targets` (solo lectura, límite 1..50, mínimo 2 caracteres) + comando `search_inventory_load_targets`.
- **F29 (done) — carga rápida + proveedor.** Cada fila tiene tilde **«Cargar»**: el operario puede **excluir** una línea (no bloquea el barrido, no es «sin resolver» y queda informada) y, si el barrido está marcado y quedan líneas sin pantalla, el asistente ofrece **«Excluir esas líneas y cargar el resto»** (antes: o no cargaba nada, o había que asignarle una ficha equivocada). Y el **PROVEEDOR** que trajo la mercancía: campo general de la carga + proveedor por línea (la línea manda) → se guarda en `products.supplier` (columna nueva, migración idempotente), se informa en el paso 3 y se ve/edita en la ficha del producto (`set_product_supplier`) y en la tabla de inventario (chip junto al nombre). Sin proveedor no se pisa el anterior.
- **Revisión adversarial de F28 aplicada** (1 bloqueante + 5 mayores + 7 menores): la **cantidad sin leer** ahora es un campo propio (`qty_issue`) que **no se borra al asignar a mano** y **bloquea la carga** (antes: asignar la ficha escribía 0 o 100.000 u. en silencio); Escape cierra el buscador y no el asistente (`onEscapeKeyDown` de Radix, el handler de React era código muerto); carrera de respuestas del buscador resuelta con contador; error de búsqueda visible; `Atrás` avisa si hay correcciones; etiqueta «asignada a mano» **por fila**; aviso de **fichas parecidas** (duplicados) en la fila; el barrido y el aviso «quedan en 0» se recalculan con las **filas vivas** (`zero_ids` en la vista previa).
- **Verificación:** `cargo test` **111/111** · build ✓ · lint 0 errores · **CDP 30/30** (`tools/verify_inventory_load.mjs`, incluye proveedor guardado y la Ayuda) · **CDP 12/12 con la LISTA REAL** (`tools/verify_inventory_load_real.mjs`: 261 líneas, carga rápida, exclusión, asignación a mano → **713 unidades**) · regresión Modelos **23/23** y padrón dueño **9/9** · `harness_security`/`harness_truth` PASS · CLI parallel ✅.
- **Datos:** la copia de trabajo quedó intacta (stock 6 u.; el proveedor de prueba de la verificación se borró después). **Ninguna feature abierta** (1-29 cerradas). Sigue todo en MODO DEV (sin release ni push).

## 2026-09-16 - F30 CERRADA: asistente para cerrar cada servicio rápido (un diálogo, cobro + entrega)

- **Feature 30 (done).** En **Servicio Técnico** cada tarjeta activa tiene un botón **«Cerrar»** (icono rayo) que abre el asistente: mira la orden, **dice qué falta** para cerrarla y pide SOLO eso, sin pasar por el formulario completo ni por tres diálogos encadenados.
  1. **¿Qué pantalla se instaló?** — solo si el trabajo incluye «Cambio pantalla» y hay pantallas en el catálogo: lista las compatibles del modelo con el **stock** de cada una y badge «agotada».
  2. **Cobro** — monto con toggle **$ / Bs.**, chips rápidos, **«Todo el saldo»**, método, comisión del Punto y referencia Zelle; la aritmética sale de **`src/lib/payment-math.ts`** (módulo COMPARTIDO con «Pago / Abono», con test de paridad `tools/payment_math_test.ts`: **595 comprobaciones OK**).
  3. **Cerrar y entregar** — un botón que registra el cobro y pasa la orden a **Entregado** (descuenta el stock de la pantalla elegida y pone la fecha), con la opción de **imprimir la orden al cerrar**.
- **Entrega con saldo:** permitida, pero el **motivo es obligatorio** (el botón queda deshabilitado sin él) y queda escrito en la orden como `Entregado con saldo ($X): motivo` — así el saldo no aparece «de la nada» en Cuentas por cobrar.
- **Antes vs ahora:** ~13 interacciones y 3 diálogos por cliente (cobrar → entregar → imprimir) → **un diálogo y como mucho dos datos** (pantalla y monto, ambos precargados). Ctrl+Enter cierra, Escape sale.
- **Nota de coordinación:** el spec lo había escrito otra sesión en paralelo; se acordó con el usuario que esta sesión lo cierra y se **reutilizó su módulo de dinero compartido** (buena idea: una sola implementación de la moneda del método vs la del campo, «todo el saldo» y Punto) en vez de duplicar la aritmética.
- **Verificación:** `tools/verify_servicio_cierre.mjs` **14/14** en vivo (crea dos órdenes de prueba y las borra al final): el asistente avisa lo que falta; la pantalla se elige con su stock; el cobro se precarga con el saldo; al cerrar la orden queda **Entregado, abonada $30, con el stock 6 → 5** y el movimiento «Servicio Entregado»; sin cobrar el botón dice «Entregar con saldo» y está **bloqueado sin motivo**; con la pantalla y el motivo, la orden queda Entregado con el motivo en las observaciones. Además: `npm run build` ✓ · lint 0 errores · `payment_math_test` 595 OK.
- Sigue todo en MODO DEV (sin release ni push). Backlog: 30 features, ninguna abierta.

## 2026-09-16 — F30 (2ª parte): cola de entregas F4, reglas compartidas y los arreglos de la revisión adversarial

- **Coordinación:** había OTRA sesión escribiendo los mismos archivos (`CierreServiceDialog.tsx`, `Services.tsx`). Se acordó con el usuario INTEGRAR: su asistente quedó como base y esta sesión aportó lo que faltaba, sin reescribir su trabajo.
- **Cola de entregas (F4)** — `CierreQueueDialog.tsx` + `lib/queue.ts`: paleta con las órdenes EN TALLER, búsqueda LOCAL (sin IPC ni debounce) por cédula, teléfono, nombre, modelo o nº de orden; badges «sin pantalla», «sin imprimir» y «falta cobrar». Botón «Cerrar entrega» + atajo `F4` (no se apila sobre otro diálogo abierto).
- **Una sola copia de cada regla:** `lib/payment-math.ts` (dinero; lo usan `PaymentDialog` y el asistente, con `tools/payment_math_test.ts` **595/595** de paridad contra las fórmulas viejas), `lib/screen-rules.ts`, `lib/queue.ts`, `lib/service-update.ts` (`updateOrderKeepingFields`), `components/ScreenPicker.tsx` y `components/FormStepper.tsx` (movidos literales desde `Services.tsx`).
- **Revisión adversarial:** `harness_review` está ROTO en este entorno (falta `tools/reviewer/parallel-review.ts`) → se hicieron **3 vueltas con 2 subagentes** (calidad y consistencia). Encontraron y se ARREGLARON 6 defectos reales de dinero/UX:
  1. **cobro duplicado** por reintento (cobro + cierre en el mismo try) → `pagoHecho` + «Reintentar cierre» que no vuelve a cobrar;
  2. **cobro anunciado como registrado cuando el cobro FALLÓ** (se usaba la intención en vez del resultado) → bandera `cobroOk` puesta tras el await + try/catch propio del cobro;
  3. **cobro fantasma** (método en $ + campo en Bs. + tasa 0 → el monto se descartaba en silencio) → `cobroImposible` + día abierto obligatorio con monto;
  4. **cambio de método sin convertir el monto** (Pago Móvil Bs. 7000 → Divisas registraba $7000) → conversión con `convertAmount` y, **sin tasa, el campo no se toca** (se bloquea con la explicación);
  5. **la cola escondía órdenes con tilde** («JOSÉ PÉREZ» no aparecía con «jose») → plegado de acentos + nº de orden sin guiones + búsqueda sin puntuación;
  6. **el teléfono real perdía contra un nº de orden que lo contiene** («0414» y la orden DEV-0414) → el trozo de nº de orden puntúa por debajo de cédula/teléfono.
- **Verificación:** `npx tsc -b` 0 errores · `npx oxlint` 0 errores · `npm run build` OK · `tools/queue_test.ts` **47/47** · `payment_math_test.ts` **595/595** · `harness_security` PASS · `harness_truth` PASS · **cero cambios en `src-tauri/`** (`cargo test` no se re-corrió a propósito: la otra sesión tenía la app viva con el lock del `target/`).
- **Pendiente (anotado en AGENTS.md):** F32 `close_service_delivery` transaccional (la atomicidad real del cierre), F31 cola/pagos en una consulta (fin del N+1 de hasta 120 `getServicePayments`), F33 vuelto y contador de entregas; dedupe vivo (lista de pantallas 40 vs 80, `esFinal` vs `isFinalized`, Referencia de Pago Móvil, entrega con saldo sin motivo por el botón viejo); y dos preexistentes: `PrintReceiptDialog.tryAutoDetect` deja `printed=1` sin imprimir y `tools/verify_servicio_cierre.mjs` escribe en la DB real abriendo el día con tasa falsa si no hay turno.
- **Cierre:** feature 30 en `done` (el aviso de «deploy readiness FAIL» es esperado: el proyecto está en **MODO DEV**, sin release ni push por instrucción del usuario).

### Cierre de F30 (revisión adversarial aplicada) — 2026-09-16

- **Revisión adversarial de F30** (independiente, por lectura): 1 BLOQUEANTE + 4 MAYORES + 9 MENORES. El bloqueante (**cambiar el método de pago no convertía el monto**: pasar de «Pago Móvil» a Zelle con 25000 en el campo registraba **$25.000**) ya estaba corregido en el merge; se aplicaron además: **comisión del Punto** inicializada en 3,5% cuando la orden ya venía con método del Punto (antes 0 → neto sin comisión y descuadre al liquidar), **«sin candidatas» ya no se trata como «no hace falta pantalla»** (`!cargandoPantallas` en el gate + aviso ámbar cuando el modelo no tiene pantallas + el error de la consulta NO se traga, porque cerrar con `screen_product_id` nulo manda el descuento al matching legacy y puede descontar otro bin), el botón de la rama «ya entregada» ahora también exige `puedeCerrar`, y el **motivo del saldo solo se anexa si hay saldo y motivo** (antes la rama yaFinal escribía «Entregado con saldo ($X): » vacío y lo repetía en cada cierre).
- **Verificado en vivo:** `tools/verify_servicio_cierre.mjs` **17/17** (incluye la **cola de entregas F4**: abre con F4, encuentra la orden por el nombre del cliente y al elegirla abre el asistente) · `tools/verify_inventory_load.mjs` **31/31** · `tools/verify_models_tab.mjs` **23/23** · `payment_math_test.ts` **595 OK** · `cargo test` **111/111** · build ✓ · lint 0 errores · `harness_security`/`harness_truth` PASS · CLI parallel ✅.
- **Nota de datos:** el padrón de la copia de trabajo se reconstruyó con el hook previsto (`REGISTRO_PHONES_DB=… cargo test -- --ignored test_manual_rebuild_phones`): con el catálogo actual (1083 productos) el padrón queda en **1134 teléfonos / 161 por revisar** (los 1079/142 eran la medición de F26 sobre otro estado del catálogo) — la expectativa de `verify_models_tab.mjs` se actualizó a esos valores.

## 2026-09-17 - F32 CERRADA: recepción guiada por estado + recordatorios de política + entregados hoy

- **Feature 32 (done).** Pedido directo del usuario: «el cliente quiere que cuando hace un registro ellos poder ver los tlf entregado hoy en servicios», «un mensaje que salga de repente cada vez que vayas a registrar un servicio, no invasivo, colores, recordándole al operador recuerda tomarle la foto al tlf», «si va imprimir preguntarle al cliente si va a pagar de una vez o al retirar, método de pago», «si es entregado salga un mensaje: ¿le tomaste la foto al entregar? Es política de la empresa» y «en el wizard que lo lleve de la mano guiándolo qué más tiene que colocar en el proceso POR ESTADO». Decisiones del usuario: **avisos solo informativos** (no se guardan imágenes), **nunca bloquean** y el asistente debe ser intuitivo.
- **Lo que quedó:** 3 columnas nuevas en `services` (`photo_in_at`, `photo_out_at`, `pay_intent`) con migración idempotente y comando **angosto** `set_service_policy` (whitelist fail-closed, la hora la estampa el backend; no se tocó el UPDATE de 25 parámetros posicionales de `update_service`); `get_services(..., date_field)` para filtrar por **fecha de ENTREGA** (antes «entregados hoy» era imposible); el wizard elige el **estado de nacimiento** (default `Recibido`: antes la orden nacía en el default silencioso `Por entregar`); módulos PUROS `src/lib/service-guide.ts` (guía por estado: qué bloquea / qué solo se avisa / paso siguiente) y `src/lib/reminders.ts` (cuándo sale cada aviso, tope 2, nunca insiste por lo resuelto); UI: `PolicyToast.tsx` (tarjeta de color vía sonner), `policy-actions.ts`, `ServiceGuidePanel.tsx` (guía compacta con «Ir» al paso del control), `EntregadosHoy.tsx` (KPI + panel de los teléfonos entregados hoy con foto/impresión/saldo) y chips en las tarjetas; ganchos al **entregar** (botón y asistente) y al **imprimir**; línea `ACORDADO: PAGA AHORA / AL RETIRAR` **solo en el talón** (el recibo principal conserva «METODO solo con pagos REALES»).
- **Revisión adversarial (2 subagentes, 2 vueltas).** `harness_review` sigue roto en este checkout. 1 BLOQUEANTE + 2 MAYORES + 18 menores, **todos arreglados**: (B) crear la orden ya en «Entregado» la dejaba **sin `date_out`, sin descuento de stock, sin garantía y fuera del panel y del libro del día** → al crear solo se ofrecen estados de taller, con el MISMO guard en el backend (`es_estado_de_taller`); (M1) la guía daba por pendiente una foto de salida ya tomada; (M2) avisos **duplicados** al entregar con «Imprimir la orden al cerrar» (id estable de sonner). Menores: chip con `photoOutIsCurrent`, `printReminders` ya no pregunta el pago de una orden cobrada, «¿va a **pagar**?» en vez de «cancelar» (en esta app «Cancelado» = pagado), el panel muestra el acuerdo o «Sin pago» (nunca el método del formulario) y la **fecha** (no una hora inventada), los «Ir» llevan al paso donde está el control, la guía es compacta (máx. 4 pendientes + «Ver todo»), los avisos **no interceptan clics** ni le roban el foco al primer campo, contraste AA, `aria-label`s, código muerto fuera y doc al día.
- **Regresión propia detectada y revertida (2ª vuelta):** exigir el monto en `save()`/`disabled` dejaba **sin poder guardar una orden de $0** (garantía/cortesía) y **hay una orden real así en la base** (DEV-0006) → el monto quedó como **AVISO** en la guía, no como bloqueo; el paso del wizard sigue pidiéndolo.
- **Verificación:** `cargo test --lib` **124/124** (6 ignorados) · `npm run build` ✓ · `tsc -b` 0 · `oxlint` 0 errores · `service_guide_test.ts` **73/73** · `reminders_test.ts` **38/38** · `receipt_acuerdo_test.ts` **30/30** · **EN VIVO** `tools/verify_recordatorios.mjs` **48/48** (aborta si no hay día abierto y borra sus órdenes de prueba) · sin regresión: `verify_servicio_cierre` 17/17, `verify_cola_entregas` 13/13, `verify_metodos_en_cobros` 15/15, `verify_wizard_metodos` 18/18 · vecinos: queue 61/61, method_picker 31/31, local_date 17/17, payment_math 595/595 · `harness_security`/`harness_truth` PASS.
- **Lecciones para futuras sesiones:** (1) una columna nueva en `services` se apenda al FINAL y se lista explícita en los 4 SELECTs (nunca `SELECT s.*`); (2) para escribir UNA señal de la orden, comando angosto con whitelist, jamás ampliar el UPDATE posicional; (3) `date_out` se guarda como **fecha sin hora** (mostrar «hora de salida» daba `--:--`); (4) los avisos de sonner con acciones van con `pointer-events-none` + `auto` en los botones y con `id` estable (si no, tapan botones y se apilan); (5) **antes de agregar un bloqueo nuevo, buscar en la base un caso legítimo que lo viole** (una orden de $0 real evitó una restricción incorrecta); (6) el bundle se re-embebe con `cargo build` tras `npm run build` — sin eso la ventana sigue con el frontend viejo (el script de verificación falló por eso antes de reconstruir).

## 2026-09-17 - F33 CERRADA: la ficha de ingreso reemplaza al aviso flotante (el asistente deja de invadir)

- **Feature 33 (done).** Textual del usuario: «siento muy invadido al registrar, el aviso debería ser más sutil, que te vaya guiando cada paso de input, no me deja ver lo que estoy registrando, necesito que sea mejor». Y su especificación de POS técnico: (1) **un tablero de la ficha de ingreso siempre visible** con los valores o «Pendiente» en 4 grupos (Datos del cliente / Ficha técnica del dispositivo / Diagnóstico y recepción / Condiciones comerciales), (2) **preguntar UN dato a la vez** con su nombre técnico y su guía corta, (3) **validar formatos** y que `corregir [campo]` vuelva al dato **sin borrar el resto**.
- **El diagnóstico del problema era el diseño anterior, no el texto:** el aviso de apertura salía **encima del formulario** justo cuando el operario empezaba a tipear (tapaba los primeros campos y le robaba el foco al primer toggle del blindaje). Se eliminó: **al abrir una recepción no hay NINGÚN aviso flotante** (`avisos=0`, comprobado en vivo). Los recordatorios de política quedan solo para los **3 momentos en que el operario TERMINA algo**: guardar la recepción (foto de entrada + acuerdo de pago), imprimir el comprobante (foto de salida) y entregar («¿le tomaste la foto al teléfono al entregarlo? es política de la empresa»).
- **`src/lib/ficha.ts` (NUEVO, regla pura con test):** `buildFicha()` arma los **16 datos** de la ficha en 4 bloques, cada uno con su valor o `null`, su estado `ok | falta | pendiente`, el **paso del formulario** donde se corrige y su aviso de FORMATO (`onlyDigits`, `ciWarn`, `phoneWarn`, `amountWarn`, `inspeccionCount`); devuelve además `next` (el dato que toca pedir AHORA con su guía), `warns`, `completa` y el progreso `done/total`. **Mapea SOLO a datos que ya existen** (nada de columnas nuevas: «trabajamos con lo que tenemos»): cliente, cédula, teléfono, dirección, modelo, color, clave del equipo (checklist `contrasena`), accesorios (chip/bandeja/forro), trabajos, falla, ítems de inspección contestados, foto de entrada, monto, acuerdo de pago, estado y técnico — más `screen` cuando el trabajo es cambio de pantalla y hay opciones.
- **`src/components/FichaIngreso.tsx` (NUEVO) reemplaza a `ServiceGuidePanel.tsx` (BORRADO):** dos líneas compactas — progreso «X/16» + **«Siguiente: Cédula del cliente — Pedile la cédula para el blindaje»** con botón **«Ir al campo»** — y «Ver ficha» que despliega los 4 bloques. Tocar cualquier dato lleva a su paso **sin perder lo cargado** (el `corregir [campo]` de la spec). Se borraron también `buildServiceGuide`/los tipos `Guide*` de `lib/service-guide.ts` (quedó solo lo que comparten varias pantallas: `STATUS_*`, `DEFAULT_NEW_STATUS`, `isFinalStatus`, `isCreatableStatus`, `photoOutIsCurrent`, `nextStep`) y `receiveIntroReminder` de `lib/reminders.ts`.
- **Dos correcciones finas de UX/regla:** `DialogContent` del wizard gana `onOpenAutoFocus` → abrir una recepción deja el cursor en **«Buscar por nombre o cédula (V-12345678)…»** (antes el foco caía en el primer toggle del blindaje, que es el paso 3); y **un monto de $0 ya no aparece como «Revisá: Presupuesto estimado»** (`amountWarn` devuelve `undefined` con total ≤ 0: una orden sin cobro o con el monto todavía sin escribir no es un error de formato — la ficha la muestra «pendiente», no «falta»).
- **Verificación:** `cargo test --lib` **124/124** (6 ignorados) · `npm run build` ✓ + `cargo build` ✓ (el bundle se re-embebe) · `tsc -b` 0 · `oxlint` 0 errores · **`tools/ficha_test.ts` 61/61 (nuevo)** · `service_guide_test.ts` **35/35** (era 73: se fueron los casos de la guía vieja) · `reminders_test.ts` **35/35** · `receipt_acuerdo_test.ts` 30/30 · **EN VIVO** `tools/verify_recordatorios.mjs` **50/50** — incluye los 6 chequeos nuevos de F33: **sin avisos flotantes al abrir una recepción** (`avisos=0`), el asistente es la ficha, **el foco arranca en Cliente**, la ficha pide el primer dato (`client`), están los 4 bloques y el monto 0 queda `pendiente` **y** sin marcarse como «falta» · sin regresión: `verify_servicio_cierre` 17/17, `verify_cola_entregas` 13/13, `verify_metodos_en_cobros` 15/15, `verify_wizard_metodos` 18/18 · `harness_security` PASS · `harness_truth` PASS.
- **Lección (vale para todo lo que se agregue al wizard):** un aviso que aparece mientras el operario **está cargando** datos es indistinguible de un aviso que le tapa la pantalla. La guía tiene que vivir **dentro** del flujo (una línea, un dato, un botón «Ir»), no flotando sobre él; y el progreso («X/16») comunica lo mismo que una barra sin ocupar lugar. Los avisos emergentes se reservan para los **cierre de acción**, donde el operario ya terminó de mirar el formulario.
- Sigue todo en **MODO DEV**: sin release ni push. **Ninguna feature abierta** (1–33 cerradas).

### F33 — 2ª vuelta de revisión adversarial (misma jornada): 1 bloqueante + 2 mayores + 6 menores

- **BLOQUEANTE — editar una orden de $0 dejaba al operario SIN botón de guardar.** En modo EDICIÓN el paso «Finanzas» tenía `done: amount > 0` y el botón Guardar **solo se dibuja en el último paso**: con una orden de $0 (DEV-0006 es real), «Siguiente» quedaba apagado, no había botón «Actualizar Servicio» y **no se podía llegar a «Cierre»** (fecha de salida, observaciones y panel de pagos). El arreglo de la vuelta anterior se había aplicado a `save()`/`disabled` **pero no al gate del PASO**. Ahora **ningún paso exige el monto** (es aviso, no gate) y el pie lo dice como NOTA en gris («Sin monto: se guarda en $0»), nunca como «Falta:». **Regresión fijada EN VIVO:** `verify_recordatorios.mjs` clickea «Siguiente» ×4 sobre una orden de $0 y exige «Actualizar Servicio» habilitado. **Probado que la prueba no es vacua:** volviendo a poner el gate viejo, el chequeo FALLA (`null`) y marca 55/56; con el arreglo, 56/56.
- **MAYOR — la ficha de CREAR mentía.** Resumía **solo el equipo 1** (`devices[0]`) y traía `hasScreenOptions:false, screenChosen:true` fijos, mientras el gate real es de TODOS los equipos (`devicesValid` + `screenOk`): decía «Lista para guardar» en verde con el guardado bloqueado y el Ctrl+Enter era un no-op **mudo** (`save()` hacía `return` sin aviso). Ahora la ficha resume todos los equipos (modelo y trabajos en `falta` si a uno le falta), agrega el blindaje que **coincide** entre equipos, la **pantalla es `required`** (es gate real: sin ella el inventario no baja o baja del repuesto equivocado) y `save()` dice «No se guardó — falta: …».
- **MAYOR — «N de N ítems revisados» con denominador falso.** `ficha.ts` usaba `Object.keys(checklist).length` = los ítems que YA tienen valor (los 2 defaults) → una orden recién abierta mostraba **«2 de 2 ítems revisados»** y parecía completa. El total ahora lo pasa la UI (`checklistTotal: CHECKLIST_ITEMS.length`, 10) y el test asertá el texto EXACTO («5 de 10 ítems revisados»), no un regex.
- **Menores arreglados:** aserción vacua en `reminders_test` (`ok(..., true)`) reemplazada por comprobaciones reales; `test_service_policy_without_open_day` (Rust) — la doc afirmaba que anotar la política no exige día abierto y **no estaba cubierto**; el tilde de foto de SALIDA al editar usa `photoOutIsCurrent` (no la foto de una entrega anterior); al CREAR ya no se ofrece la foto de SALIDA (estampaba una foto de la recepción y, entregando el mismo día, `photoOutIsCurrent` la daba por válida y se callaba el aviso); el conmutador «Entregados» con el filtro «Activos en taller» daba lista vacía garantizada → ahora ajusta el estado a Entregado y lo devuelve al volver a «Recibidos» (solo si el ajuste fue automático); variable de test en snake_case (0 warnings).
- **Verificación final:** `cargo test --lib` **125/125** (6 ignorados, 0 warnings) · `tsc -b` 0 · `oxlint` 0 errores · `npm run build` + `cargo build` ✓ · `ficha_test` **66/66** · `service_guide_test` 35/35 · `reminders_test` **38/38** · `receipt_acuerdo_test` 30/30 · **EN VIVO** `verify_recordatorios.mjs` **56/56** (6 chequeos nuevos: sin avisos flotantes, ficha con los 4 bloques y las 16 filas, el dato pedido, el foco en Cliente, el monto 0 como aviso, «corregir sin borrar», y el bloqueante del botón) · sin regresión: `verify_servicio_cierre` 17/17, `verify_cola_entregas` 13/13, `verify_metodos_en_cobros` 15/15, `verify_wizard_metodos` 18/18 · `harness_security` PASS · `harness_truth` PASS (build pass).
- **Lección:** un `disabled`/gate de PASO es tan bloqueante como un `disabled` de GUARDADO — al declarar «X no bloquea», hay que revisar **todos** los gates que tocan ese dato (botón, `save()`, `steps[].done`, `goTo`/`stepCurrent`), no solo el del guardado. Y una prueba de regresión se valida **reponiendo el bug**: si con el bug viejo pasa igual, no sirve.

## 2026-09-17 - F34 y F35 CERRADAS: técnico rápido en la tarjeta + fecha del pago (que la caja cuadre)

- **F34 (done) — cambio rápido de técnico.** Pedido textual: «le dé clic al nombre del técnico o a la letra [iniciales] que tiene la tarjeta [y] me dé la opción cambiar técnico rápido. Se refleje obviamente». El círculo de iniciales + el nombre del encabezado de cada tarjeta son **un solo botón** (`data-tech-quick`) que abre un **selector rápido** con los técnicos del padrón (el actual marcado con ✓) y **«Sin asignar»** (`data-tech-option`). Se guarda por la **única vía permitida** (`updateOrderKeepingFields`, que no pisa ningún otro campo), la tarjeta lo refleja al instante y sale un toast. Sin técnico la tarjeta dice «Asignar». Por equipo en multi-equipo y válido en cualquier estado.
- **F35 (done) — fecha del pago.** Pedido textual: «cliente[s] dejan el tlf a reparar pero pagan ese mismo día y no lo notifican; [que] pueda editar o agregar la fecha de ese pago — ese mismo día, no el día anterior — porque a veces tiende [a] faltar dinero o sobrar al cerrar esa caja». Antes `add_service_payment` **no recibía fecha** (default `datetime('now')`): un cobro del 16 anotado el 17 hacía cerrar el 16 con falta y el 17 con sobra. Ahora el diálogo tiene **«Fecha del pago»** y el backend la recibe; `update_service_payment_date` (comando **angosto**) corrige la fecha de un pago ya anotado y **recalcula `paid_amount`** (la tasa del día del pago es la que convierte Bs→USD). Guardas: no futura, calendario real, y **el día tiene que ser un turno ABIERTO** — si está cerrado el error dice el camino real (Libro Diario → Cierres → ↺ abrir ese día, anotar y volver a cerrarlo), así **nunca queda un arqueo guardado mintiendo**.
- **Decisión medida en vivo:** la fecha por defecto es la del **TURNO ABIERTO** (`getActiveDay().close_date`), no «hoy» a ciegas — en la copia de trabajo el turno abierto era del 14-08 con hoy 17-09, y un abono «de hoy» no habría caído en ninguna caja. La primera versión del gate rechazaba el pago normal por eso; lo destapó una sonda de CDP antes de dar la feature por buena.
- **Revisión adversarial (subagente): 2 BLOQUEANTES + 5 MAYORES + 7 menores, TODOS arreglados** (detalle en AGENTS.md). Los dos bloqueantes eran de F34 y ambos **movían plata**: (1) cambiar el técnico de una orden **entregada** reescribía `date_out = hoy` (el monto se mudaba de la caja del día de la entrega a la de hoy, se reiniciaba la garantía de 7 días y entraba en «Entregados hoy») — `updateOrderKeepingFields` mandaba `dateOut: ''` y el backend lo lee como «entregá hoy»; (2) **«Sin asignar» no desasignaba** (`?? ` con `null` caía al id viejo: `technician` NULL + `technician_id` del anterior, la tarjeta no cambiaba y el toast mentía). Mayores: los pagos nuevos **perdían la hora** (el del día la conserva; el retroactivo guarda solo la fecha y el detalle muestra «—»); el remedio del error de día cerrado **no se podía completar** (con dos turnos abiertos el único «Cerrar Día» apuntaba al más reciente → **botón «Cerrar» por fila** en Cierres); **regresión del asistente de cierre F30** (mandaba «hoy» y con el turno en otro día rechazaba el cobro y el operario no podía cobrar **ni** entregar); **doble abono** por Ctrl+Enter/reintento.
- **Verificación:** `cargo test --lib` **126/126** (test nuevo `test_payment_date_lands_on_the_right_day`: pago de ayer en la caja de ayer y no en la de hoy, mover la fecha mueve la plata conservando `paid_amount`, **conversión en Bs con la tasa DEL DÍA** —Bs 4.050 a 40,5 = $100, no $81—, hora del pago del día, retroactivo sin hora, y los 6 rechazos con su mensaje) · `tsc -b` 0 · `oxlint` 0 errores · build ✓ · **EN VIVO `tools/verify_tecnico_y_fecha_pago.mjs` 25/25** (incluye «Sin asignar» desasignando de verdad y una orden entregada el 05-01 que **conserva su `date_out`** tras cambiar el técnico: la prueba se validó reponiendo el bug) · sin regresión: verify_recordatorios 56/56, verify_servicio_cierre 17/17, verify_cola_entregas 13/13, verify_metodos_en_cobros 15/15, verify_wizard_metodos 18/18 · `harness_security`/`harness_truth` PASS.
- **Pendiente anotado (feature 36):** el tope de la devolución en Bs se compara con la tasa de HOY mientras `paid_amount` usa la del día de cada abono → devolver los mismos Bs. deja un saldo fantasma cuando la tasa cambió (el invariante «paid_amount nunca negativo» sí se cumple). Es una **decisión de negocio** (¿la devolución salda en Bs. o en $?), no un bug que se arregle en caliente.
- Sigue todo en **MODO DEV**: sin release ni push. Abierta: **36** (decisión de negocio).

## 2026-09-17 - F36 CERRADA: devoluciones y saldo POR MONEDA (la tasa correcta: la del día del cobro)

- **La pregunta del dueño:** «¿qué sería lo correcto? que sea lo más eficiente, puede ser la de hoy o la de ese día del registro». **La respuesta implementada:** (1) **para el TOPE, ninguna tasa** — se devuelve como máximo lo que **netamente entró en ESA moneda** (Bs con Bs, $ con $); (2) **para el VALOR en dólares de lo abonado, la tasa del día en que ENTRÓ la plata** (el día del registro de ese pago), **nunca la de hoy**; (3) `paid_amount = neto_USD + (neto_VES / tasa del primer ingreso en Bs)`; (4) **la caja no se toca** (suma montos crudos por método y día).
- **El bug que arregla (medido):** abono de Bs. 4.050 a 40,50 (= $100) + devolución de los MISMOS Bs. 4.050 a 50 (= $81) → la orden seguía «debiendo» **$19** y **el recibo lo imprimía**, aunque el cliente ya estaba saldado en bolívares. Ahora devolver todo lo cobrado **salda la orden** (`paid_amount = 0`), sin importar la tasa.
- **Una sola implementación de la regla:** `paid_net_by_currency` / `ves_rate_for_net` / `refundable_in` / `recalc_paid_amount` (Rust) y `src/lib/refund-math.ts` (TS, con test de paridad `tools/refund_math_test.ts` 24/24). El diálogo dice **cuánto se puede devolver en la moneda del cobro** («Disponible para devolver en Bs.: 4.050,00») y ya no depende de la tasa de hoy.
- **Revisión adversarial (subagente, dinero): 2 BLOQUEANTES + 3 MAYORES + 7 menores, TODOS arreglados.** **(B1) La migración de arranque seguía usando la fórmula VIEJA:** `db.rs::init()` tenía su propio `UPDATE services SET paid_amount = …` y, como corre en **cada arranque**, F36 se revertía al reiniciar (medido con dos abonos en Bs en días con tasas distintas: **$211,11** con la regla nueva vs **$190** con la vieja) → `init()` ahora llama a `recalc_paid_amount` y el test `test_migration_keeps_f36_net_rule` lo fija, **comprobado reponiendo el bug** (con la fórmula vieja el test falla: «tras reiniciar la app el saldo NO puede volver a la regla vieja ($190): 190»). **(B2) `paid_amount` podía quedar NEGATIVO** por 3 caminos: borrar el cobro después de devolver; **la tolerancia de 0,5 repetida con el neto en 0** (regresión que introdujo la primera versión de F36, deriva sin límite); y un `add_service_payment` con **monto negativo** (devolución encubierta que salteaba el tope y restaba de la caja) → 4 guardas: `disponible > 0.005` antes de la tolerancia, monto del pago > 0, `delete_service_payment` rechaza borrar un cobro ya devuelto, y **piso en 0** en el recálculo. Mayores: `confirmNoMoney` **se filtraba entre órdenes** (marcaba Devuelto una orden PAGADA sin devolver la plata), la **devolución se fechaba HOY** en vez del **turno abierto** (medido en la base real: −$20 con fecha 17-09 y el turno abierto del 14-08 → ese egreso quedaba fuera del cierre), y «Devuelto/Cancelado no acepta pagos» solo vivía en la UI. Menores: cambiar de método ya no deja el monto anterior con otro rótulo, el tope se muestra en el mismo formato local en los dos lados (`fmt_miles`), `ORDER BY` en el fallback de la tasa, y los doc-strings alineados con el SQL.
- **Medición sobre la base REAL antes de tocar nada:** `node tools/snapshot_db.mjs --src dev_registro.db --out backup/verif_f36.db --force` + `node tools/audit_paid_amount_f36.mjs` → **10 órdenes con movimientos, 4 con devoluciones, 0 cambian de `paid_amount`**: la migración es inocua sobre los datos del local (todos los cierres tienen la misma tasa 748,79, que es también por lo que el bloqueante 1 todavía no se veía en los saldos).
- **Verificación:** `cargo test --lib` **128/128** (test nuevo `test_refund_by_currency_net` con 12 pasos + `test_migration_keeps_f36_net_rule`) · `refund_math_test` **24/24** · `ficha_test` 66/66 · `service_guide_test` 35/35 · `reminders_test` 38/38 · `receipt_acuerdo_test` 30/30 · `payment_math_test` 595/595 · **EN VIVO `verify_tecnico_y_fecha_pago.mjs` 30/30** (5 chequeos nuevos de F36) · regresiones 56/56, 17/17, 13/13, 15/15, 18/18 · `tsc -b` 0 · `oxlint` 0 errores · build ✓ · `harness_security`/`harness_truth` PASS.
- **Pendiente para el dueño (decisión de negocio, no bug):** valuar todo el neto en Bs con la tasa del **primer** ingreso hace que, si la tasa subió entre abonos, el abonado en $ quede a la tasa más vieja (el saldo sale $38,89 en vez de $60; $79 al cambio de hoy). Lo correcto del todo sería **mostrar el saldo en la moneda en que se cobró** cuando todos los movimientos son de la misma moneda. Anotado como **feature 37** junto con el `isBsMethod` heurístico.
- Sigue todo en **MODO DEV**: sin release ni push. Abierta: **37** (menor).


## 2026-09-17 - F38 + F39 CERRADAS: que TODO cuadre como un POS venezolano (y los 12 hallazgos de la revisión adversarial)

- **El pedido del dueño:** «¿cómo debería ser a nivel profesional que todo cuadre? Recuerda es Venezuela, todo tiene que cuadrar como un sistema de POS de venta». **La regla madre: la caja cuadra por MONEDA y por MÉTODO; la tasa BCV solo INFORMA, nunca cuadra.** Decisión de alcance: **A + B ahora, C después** (C = el libro único de movimientos de caja + log de auditoría, registrado como **feature 40**).
- **F38 — el saldo se dice en la MONEDA DEL COBRO.** Si el cliente viene pagando en bolívares, la tarjeta, el diálogo de abono y el comprobante dicen el saldo **en Bs.** (con la tasa del turno abierto) y el $ va entre paréntesis como verdad contable: «Bs. 72.879,00 ($97.33)». **Regla pura `src/lib/order-balance.ts`** (`orderBalance`/`balanceLabel`): la moneda del cobro sale del **neto por moneda** de los movimientos (si TODOS son de una moneda el saldo va en esa; con cobros mixtos o sin cobros en $); **sin tasa cargada no se inventa ninguna equivalencia**; **la deuda NO se revalúa** (el $ no cambia porque se mueva la tasa). Se usa en `PaymentDialog` (`data-field="saldo"` en las cuatro ramas), el badge de la tarjeta y el banner de la orden multi-equipo (`Services.tsx`, con `tasaDia` del `getActiveDay()` que ya existía) y la línea nueva **`FALTA Bs.`** del comprobante (recibo + talón).
- **F39 — el arqueo se mira POR MONEDA, en dos columnas.** La lista de Libro Diario → Cierres mostraba **un solo número** (`difference = faltante_$ + faltante_Bs / tasa`) con umbral ±0,5: **como semáforo mentía** (con la tasa moviéndose un descuadre de Bs. parecía enorme y un faltante real de $0,40 se diluía). Ahora hay **«Diferencia $» y «Diferencia Bs.»** (`data-diff` + `data-ok` por moneda) con la tolerancia de cada moneda (0,5 = lo que se redondea al contar el cajón). **Regla pura `src/lib/cash-closing.ts`** (`closingDifference`, `closingLabel`, `puntoDifference`, `sinContar`), que **lee los esperados de las columnas GUARDADAS del cierre**: un cierre viejo se lee igual aunque después cambie algo.
- **PRIMERA VUELTA adversarial (subagente, dinero + consistencia): 1 BLOQUEANTE + 4 MAYORES + 7 menores, TODOS arreglados.**
  - **(BLOQUEANTE) «Diferencia $» daba SIEMPRE 0:** el diálogo de cierre mandaba `actual_cash_usd = esperado`, así que el arqueo nunca podía detectar un faltante de divisas → input nuevo **«Divisas contadas ($)»** y ahora se manda lo contado.
  - **(MAYOR, medido en vivo) El Bs. mostrado y el Bs. cobrado diferían en 1 bolívar:** la pantalla decía `Bs. 72.880,00` y el chip «Todo el saldo» cobraba 72.879, porque `orderBalance` redondeaba el saldo a centavos ANTES de multiplicar por la tasa y `paid_amount` viene de convertir Bs. a dólares ($2,6711, no redondo) → `bs = round(usdRaw × tasa)`, con **prueba de paridad contra `suggestAmount` Y `saldoChipValue`** (y el $ que se muestra sigue redondeado a centavos).
  - **(MAYOR) El campo del monto no seguía la moneda del cobro:** una orden que se venía pagando en Bs. abría el campo en `$` → arranca en **Bs.** (`cobroEn`), salvo que el operario lo cambie a mano (`curTouched`: nunca se le pisa la elección).
  - **(MAYOR) Cobrar más que el saldo no avisaba nada:** aviso ámbar `data-field="aviso-excedente"` («quedaría a favor del cliente $X»). **No bloquea** (el cliente puede redondear o adelantar plata), pero se ve.
  - **(MAYOR) El recibo juzgaba el cobro en Bs. por montos BRUTOS:** un abono en Bs. devuelto por completo seguía imprimiendo `FALTA Bs.` mientras la pantalla decía el saldo en $ → el recibo usa **`netByCurrency`** (la misma regla, UNA implementación) y el monto impreso va **redondeado al bolívar** (`FALTA Bs.: Bs. 21.460,00`, no 21.460,32).
  - **Menores:** un cierre **sin arqueo guardado** (`actual_*` en 0) mostraba como diferencia **todo el efectivo del día** → se rotuló «sin arqueo» (y en la SEGUNDA vuelta se corrigió esa decisión: ver abajo — el número no se esconde nunca); el signo se imprimía DESPUÉS del símbolo (`$-2.00`) → **`-$2.00`** / `-Bs. 1.000,00`; las **ventas en Bs.** se guardaban con centavos de bolívar (descuadre contra la tolerancia de 0,5 Bs.) → se redondean al bolívar y **el número que se muestra es el que se guarda**; `closingLabel`/`puntoDifference` no los usaba nadie (había dos restas propias en el diálogo de «Liquidar») → cableados; `colSpan` del estado vacío de Cierres quedó en 11 con 12 columnas; el estado de la orden anterior podía verse un instante al abrir el abono de otra (`setPayments([])` al abrir).
- **Lo que NO se cerró (anotado, no oculto):** con más de **120 filas** en la lista de servicios no se piden los movimientos (N+1 queries), así que se pierde el chip del método real y el **orden** de las monedas — **no es un error de plata**: sin datos `balanceLabel` dice las DOS monedas («$80.00 (Bs. 59.903,20)») y el operario sigue viendo el Bs. Arreglarlo bien es la **feature 40**. También queda declarado el techo de F36: valuar todo el neto en Bs. con la tasa del **primer** ingreso subestima el abonado en $ si la tasa subió entre abonos (el saldo en Bs. no se ve afectado).
- **Verificación de la 1ª vuelta:** `node tools/pos_cuadre_test.ts` **60/60** · `node tools/receipt_acuerdo_test.ts` **52/52** · **EN VIVO `tools/verify_tecnico_y_fecha_pago.mjs` 40/40** · regresiones EN VIVO: `verify_recordatorios` **56/56**, `verify_servicio_cierre` 17/17, `verify_cola_entregas` 13/13, `verify_metodos_en_cobros` 15/15, `verify_wizard_metodos` 18/18 · `cd src-tauri && cargo test --lib` **128/128** (6 ignorados) · `tsc -b` 0 · `oxlint` 0 errores · `npm run build` + `cargo build` ✓ · `harness_security` PASS · `harness_truth` PASS.
- **Una prueba que fallaba por el RECORTE, no por el producto:** `verify_recordatorios` recortaba el texto de la fila a 120 chars y, con F38, la fila dice además «Falta $30.00 (Bs. 22.464,00)» → «Sin imprimir» quedaba fuera del recorte. Se amplió a 240 con un comentario que explica por qué (el diagnóstico mostraba el texto completo y el chip estaba ahí).
- **SEGUNDA VUELTA adversarial (subagente, dinero + consistencia): 1 BLOQUEANTE + 4 MAYORES + 7 menores, TODOS arreglados** — y encontró un bloqueante REAL que la primera vuelta no vio:
  - **(BLOQUEANTE) El ESPERADO de la caja contaba DÓLARES como BOLÍVARES.** `compute_daily_totals` tiene una rama que **presume** el cobro de una orden ENTREGADA sin ningún pago registrado (el cliente pagó en el mostrador y nadie lo anotó) y metía `services.amount` —que es SIEMPRE en dólares— crudo en el bucket del método del formulario: una orden de $20 con método «Efectivo Bs» sumaba **Bs. 20** en lugar de Bs. 14.975,80 (tasa 748,79) → el esperado del cajón en Bs quedaba **749 veces corto** y la «Diferencia Bs.» que estrena F39 mostraba un descuadre inventado. La rama ahora se marca (`presume` = 1 en el UNION) y se **convierte con la tasa del día de la entrega**; **sin tasa no se presume nada** (nada estimado en la caja). Test Rust nuevo `test_presumed_counter_charge_uses_the_day_rate`.
  - **(MAYOR) El asistente de cierre decía el saldo en Bs. y el campo del monto arrancaba en dólares:** «Falta cobrar Bs. 72.879,00 ($97.33)» al lado de un campo en dólares sin moneda en la etiqueta → el operario copiaba el número y se guardaban **72.879 dólares**. Ahora el asistente usa el **mismo efecto de F38** que el diálogo de abono (`cobroEn` + `curTouched`) y la etiqueta dice «Monto ($)/Monto (Bs.)». Verificado EN VIVO.
  - **(MAYOR) El toggle del monto se contaminaba con los pagos de la orden ANTERIOR:** los efectos corren después del render, así que al abrir la orden B el efecto de F38 leía el array de la A y pisaba la moneda correcta (una orden de Pago Móvil abría el campo en $ → 5000 tecleados = Bs. 3.743.950). Se exige que los movimientos sean **de esta orden** y el historial se limpia al CERRAR el diálogo.
  - **(MAYOR) La primera versión de `hasArqueo` TAPABA un descuadre real:** esconder la diferencia de un cierre con el arqueo en 0 ocultaba que faltaba **todo** el cajón (cerrar con las dos casillas en 0, o abrir el diálogo cuando `getDailyTotals` falla). Ahora la diferencia **se muestra SIEMPRE** y `sinContar` solo agrega la marca «sin contar» con el remedio en el `title`; además **no se puede cerrar el día sin haber leído los totales** (botón deshabilitado + mensaje).
  - **(MAYOR) Cobrar de más una orden YA SALDADA no avisaba nada:** el aviso ámbar ahora sale también ahí.
  - **Menores:** un solo criterio de «saldado» (`saldoChipValue` corta en 0,005 como `suggestAmount`, y `orderBalance` devuelve `bs = 0` cuando está saldada — antes el chip ofrecía 3 Bs. mientras el campo se autocompletaba en 0 y el texto decía «Sin saldo»); el **banner del grupo** ya pasa `pagosGrupo` en las DOS líneas (decía el saldo en dólares arriba y en Bs. abajo); la **diferencia del diálogo de cierre** sale de `closingDifference` y el Punto declarado al cerrar de `puntoDifference` (no de restas propias); **el recibo usa `orderBalance`** (una sola regla para pantalla y papel); y una aserción **VACUA** de la prueba del recibo (`includes('FALTA')` pasaba siempre porque «FALTA Bs.» contiene «FALTA») se reemplazó por el **monto exacto** (`FALTA: $ 28.66`).
  - **Decisión de negocio declarada (no bug):** los cobros **digitales** (Zelle, Pago Móvil, Transferencia, Punto) entran al arqueo **con su monto esperado** — no están en el cajón, se verifican en el banco; por eso la «Diferencia Bs.» mide el **efectivo contado**, y el diálogo de cierre ya lo dice. La conciliación bancaria por referencia queda en la feature 40.
  - **Lección de las PRUEBAS EN VIVO (no del producto):** `verify_servicio_cierre` fallaba 1 de cada 3 veces porque esperaba 1500 ms fijos y la consulta de pantallas compatibles **se encola detrás de las consultas de la lista de servicios** (hasta 120 llamadas IPC de movimientos). Ahora **se espera la condición, no el reloj** (`waitFor`): 17/17 tres veces seguidas. Regla general anotada.
- **Verificación final (tras la 2ª vuelta):** `cargo test --lib` **129/129** · `pos_cuadre_test` **66/66** · `receipt_acuerdo_test` **52/52** · `refund_math_test` 24/24 · `ficha_test` 66/66 · `service_guide_test` 35/35 · `reminders_test` 38/38 · EN VIVO: `verify_tecnico_y_fecha_pago` **41/41**, `verify_recordatorios` 56/56, `verify_servicio_cierre` 17/17 (×3), `verify_cola_entregas` 13/13, `verify_metodos_en_cobros` 15/15, `verify_wizard_metodos` 18/18 · `tsc -b` 0 · `oxlint` 0 errores · builds ✓ · `harness_security`/`harness_truth` PASS.
- Spec: `tools/progress/specs/F38-F39-cuadre-pos-venezuela.md`. Sigue todo en **MODO DEV**: sin release ni push. Abiertas: **37** (heurística de `isBsMethod`) y **40** (libro único de movimientos de caja + auditoría).

---

## F41 — Inventario RÁPIDO en cada pestaña (2026-09-17, MODO DEV) — CERRADA

**Pedido del dueño:** «optimizar la app, que sea rápida cuando entra inventario, cada pestaña/sección».

**Medido ANTES (release, copia de la base con 1126 productos y 1136 teléfonos, `tools/bench_inventory_ui.mjs`):** entrar a Inventario 400 ms · pestaña **Modelos 896 ms** · buscador de modelo 438 ms · repuestos compatibles 470 ms · Movimientos 234 ms · volver a Productos 340 ms. En el backend cada pestaña rehacía cálculos derivados del catálogo: índice de teléfonos 118 ms, totales de los 1135 teléfonos ~140 ms, KPIs 113 ms y compatibilidad parseada 240 ms **por consulta**; y el frontend esperaba 200 ms de rebote en TODA consulta, tapaba con esqueleto lo que ya estaba en pantalla y consultaba el padrón al montar el combobox de modelo.

**Qué se hizo.** (A) **Memoria corta del catálogo** (`src-tauri/src/cache.rs`): índice de repuestos por teléfono, filas del padrón (de las que se derivan los totales del selector de modelo), KPIs y catálogo con la compatibilidad ya parseada; se calcula a lo sumo una vez por versión de la base y la versión la da SQLite: par `(SELECT total_changes(), PRAGMA data_version)`. (B) **Frontend:** el rebote de 200 ms quedó SÓLO para lo que se escribe (la primera carga y los filtros salen en el acto), esqueleto sólo cuando no hay nada que mostrar (con «· actualizando…» y `data-refreshing` cuando ya hay tabla), el combobox de modelo consulta cuando se lo usa y **precalentado en tiempo libre** al abrir el módulo. **Fuera de alcance por decisión:** dejar las pestañas montadas (keep-alive) — obligaría a reescribir todos los verificadores en vivo que cuentan filas de `table tbody`; con la memoria, volver a una pestaña cuesta ~60 ms y lo que se pierde son los filtros (anotado).

**Medido DESPUÉS (mismas condiciones):** Inventario **~100 ms** · Modelos **~120 ms** · repuestos compatibles **~110 ms** · Movimientos **~30 ms** · volver **~60 ms** · buscador de modelo ~200 ms (de los cuales ~180 son el rebote intencional al escribir).

**Revisión adversarial (2 subagentes) — 2 BLOQUEANTES y varios menores, TODOS arreglados:** (1) la versión de la memoria no veía las escrituras de OTRA conexión (dos ventanas de la app, o `tools/` con la app abierta) → el par con `data_version`, fijado con `test_catalog_cache_sees_writes_from_another_connection` y comprobado en vivo (1136 → insert ajeno → 1137 → delete → 1136, la pantalla siempre igual a la base); (2) el desplegable del buscador de modelo podía quedar en «Buscando…» para siempre (estado `loading` trabado si se abortaba el efecto) → el desplegable ahora se pinta según `optionsForQuery`; el precalentado se recortó a las DOS llamadas que construyen la memoria; `changeSearch` de Modelos ya no dispara una consulta tirada con el texto viejo estando en página > 0; los verificadores nuevos esperan CONDICIONES (no relojes), abortan si falta `REGISTRO_DB` y limpian sus restos en un `finally`.

**Verificación:** `cargo test --release --lib` **132/132** (3 tests nuevos: invalidación por escrituras propias, por OTRA conexión, y semántica del buscador de repuestos) · `tsc -b` 0 · `oxlint` 0 errores · `npm run build` + `cargo build --release` ✓ · `harness_security` PASS · `harness_truth` PASS · EN VIVO: **`tools/verify_inventario_rapido.mjs` 10/10** (compara la pantalla contra la BASE leída aparte con `node:sqlite`), **`verify_models_tab` 23/23**, **`verify_screen_brand_gate` 23/23** (el gate de marca del servicio, lo que más podía romper el catálogo memorizado), y todos los `*_test.ts` en verde (pos_cuadre 66/66, receipt 52/52, ficha 66/66, service_guide 35/35, reminders 38/38, refund 24/24, payment_math 595/595, queue 61/61, fechas 17/17, method_picker 31/31).

**Hallazgo aparte (NO de esta feature, queda anotado):** `delete_product` no limpia el padrón (`rebuild_phones` sólo inserta/actualiza por clave) → borrar un producto deja su teléfono en Modelos con 0 repuestos.

**Herramientas nuevas que quedan:** `tools/bench_inventory_ui.mjs` (mide en vivo, por pestaña, lo que tarda en verse el dato) y `tools/verify_inventario_rapido.mjs` (la memoria no miente). Abiertas: **37** y **40**.

---

## F42 — La devolución vuelve POR DONDE ENTRÓ la plata (2026-09-17, MODO DEV) — CERRADA

**Bug de plata reportado por el dueño con la app en la mano** («devolví 2 dólares en Bs… y al cerrar caja me sale 43 dólares efectivo»). Diagnóstico sobre una copia: los **$43 eran correctos** (los dos cobros en efectivo dólares del día) y el defecto estaba en la DEVOLUCIÓN: la orden se cobró $3 en efectivo + **Bs. 1.697 por Pago Móvil** (=$2) pero la devolución quedó anotada en **«Punto de Venta (Bs)»** porque el diálogo **proponía el método del FORMULARIO** (`service.payment_method` = sólo lo que se esperaba cobrar). Efectos: **Punto −Bs. 1.697** (imposible) y, como la fila del Punto sólo se dibuja con esperado > 0, **la devolución desaparecía del cierre**.

**Arreglo:** reglas puras en `src/lib/refund-math.ts` (`incomeByMethod`, `refundMethodDefault`, `refundMethodProblem`, `isCashMethod`, `incomeSummary`) + **gate fail-closed en el backend** (un método que no cobró nada en esa moneda no puede devolver; el error dice por dónde entró; los de cajón sí pueden, con aviso) + el diálogo propone el método real, muestra «Entró por: …» y ofrece el correcto a un toque + **el Libro y el cierre ya no esconden un esperado ≠ 0** (`|x| > 0.005`) y muestran lo devuelto (chip en la fila del día, KPI «Devuelto», línea en el cierre) con los campos nuevos `DailyTotals.refund_usd/refund_bs`.

**Datos del día corregidos** por los comandos de la app (respaldo `backup/registro_pre_fix_devolucion_20260917.db`): borrado el movimiento mal anotado y reanotado por Pago Móvil → Divisas $43, Pago Móvil 0, Punto 0, «Devuelto Bs. 1.697,00».

**Verificación:** `cargo test --release --lib` **133/133** · `refund_math_test.ts` **45/45** · EN VIVO `verify_devolucion_metodo.mjs` **7/7** (pedido de prueba borrado al final) · Libro y cierre verificados en vivo · `tsc -b` 0. Spec: `tools/progress/specs/F42-devolucion-por-donde-entro.md`.

---

## F43 — 🚀 RELEASE OFICIAL v0.4.0: publicar la actualización garantizando que la migración NO toca los datos del local (2026-09-18) — CERRADA

**Pedido del dueño:** «vamos a lanzar la actualización oficial de una vez **siempre y cuando no se dañen los datos de los usuarios**: cuando se hagan los cambios de sus db se mantenga y sus ventas registradas».

**Estado encontrado:** la última release publicada era la **v0.2.5 (24/8)** — todo el trabajo de septiembre (inventario completo + F30–F42) estaba sin publicar — y el instalador **empaquetaba la base de trabajo del taller** (`../registro.db`: 4 órdenes, 6 abonos, 1 cliente real, un día abierto y un WAL de 1,3 MB) en un repo PÚBLICO. El gate de release abortaba con 9 bloqueantes.

**Publicado:** https://github.com/shaman2527/Service_Tecnico/releases/tag/v0.4.0 — setup firmado (6,39 MB) + `.sig` + `latest.json`; el endpoint del updater sirve la 0.4.0 con la MISMA firma del build local. `registro.db` del taller **nunca se tocó** (ni se publicó): `tauri.conf.json` ahora empaqueta `backup/plantilla_candidata.db`, regenerada y verificada después del build (1087 productos, 950 con precio, 704 unidades, 0 filas de transaccionales/clientes, sin día abierto, PIN inicial). Además se arregló que el PIN de la plantilla viajaba con el **PIN real del dueño** (hasheado) porque el generador solo lo creaba si faltaba.

**La condición del dueño como prueba repetible:** hook Rust `test_manual_migrate_db` (migración REAL: `Database::new` → `init()`, con centinela) + `node tools/verify_migracion_datos.mjs` (copia con VACUUM INTO, foto antes/después columna por columna) + `node tools/verify_migracion_negativa.mjs` (prueba por comportamiento: inyecta daño y exige FAIL) + `node tools/verify_release_publicado.mjs` (lo publicado). **Medido:** 50/50 base real (migración 118 ms, ni una celda), 52/52 base de la era 0.2.5 con ventas y clientes, 68/68 base de julio, 5/5 daños detectados.

**La revisión adversarial (2 subagentes) encontró 2 BLOQUEANTES en la propia prueba** — y es la lección más importante: (1) la prueba podía dar **falso verde** porque solo miraba el exit code de `cargo test` (un filtro que no matchea sale 0 con «0 filtered out» y la comparación era la copia contra sí misma): ahora exige la salida del hook en un archivo, `test result: ok. 1 passed` y el centinela; (2) **las filas BORRADAS pasaban como intactas** (se recorría solo el «después») y quedaban afuera `inventory_movements`, compras y casi todas las columnas de `products`: ahora se comparan conteos y la unión de ids. Mayores: la plata recalculada no tenía umbral (ahora el **saldo** con tolerancia de un centavo, tasa de cierre que no se pierde, total de cierre que no cambia de signo); el gate imprimía PASS sobre consultas que fallaban (ahora todo error es bloqueante y se exige el esquema); el gate no miraba el VALOR del PIN. Menores: el gate lee la plantilla de `bundle.resources` (una sola fuente de verdad), la lectura de `products` avisa si la base es vieja en vez de morir, el deduplicador de aprendizajes ya no descarta niveles ni borra backticks, y el PIN viejo no se imprime.

**Hallazgo aparte:** `tools/progress/patterns.md` había llegado a **310 MB** (el consolidador duplicaba el contenido en cada corrida: 20 aprendizajes únicos, uno repetido 524.288 veces = 2^19). El `git add -A` del release lo metió al historial → deduplicado con `tools/dedupe_patterns.mjs` (310 MB → 21 KB, conservando los 26 aprendizajes únicos, 14 de ellos ausentes en git), blob quitado del commit y `.git` de 14,5 MB → 1,5 MB. El gate ahora bloquea un archivo de más de 100 MB.

**Verificación:** `cargo test --release --lib` **133/133** (8 ignorados) · 10 pruebas puras **1006/1006** · `tsc -b` 0 · `oxlint` 0 errores · `npm run build` ✓ · `harness_security` PASS · `harness_truth` PASS · gate de release **0 bloqueantes** · `verify_release_publicado` **21/21**.

**Pendiente:** cargar el precio de los 123 SKU con stock (333 unidades) sin precio —son modelos más nuevos que la lista del local, hoy en una **excepción acotada y auditable** con topes en `tools/release_excepciones.json`— y después borrar la excepción; instalar la 0.4.0 en la PC del local y confirmar el mostrador (PIN, abrir día, venta, entrega, cierre, ticket).

---

## F44 — «Trabajos hechos»: contar pantallas y trabajos por día (2026-09-20, MODO DEV)

**Pedido del dueño:** «el cliente me pregunta cuántas pantallas hice hoy o un día en específico… cuando cambio el filtro debería darme los servicios que he hecho, cambio de pantalla por ejemplo o cambio de pin, y **en entregado no me aparecen**. El filtro predeterminado debería ser **todos los estados**; el que tengo actual es "activos en taller", no debería ser ese… quiero saber qué servicios hago, que todo salga bien reflejado».

**Diagnóstico medido (no supuesto):** la lista abría en el sentinel `__activos__` y con la base real (`registro.db`: 4 órdenes, `Entregado`×3 + `Devuelto`×1) **abría VACÍA**; los contadores de trabajos se calculaban **solo sobre las órdenes activas** (`enTaller`) y se dibujaban solo con count > 0 → al filtrar por entregado **desaparecían todos los chips** (el defecto textual); el contador y el filtro usaban **reglas distintas** (el chip podía mentir); «Total Equipos»/«Monto Total» contaban **otra lista**; los trabajos de **texto libre** («Otro» → «Cambio de pin de carga») **no tenían contador**; y el estado vacío decía «Sin servicios registrados» **también cuando el culpable era el filtro**.

**Qué se implementó (cero cambios en Rust y en la base):** módulo puro `src/lib/service-report.ts` (`foldWork`, `workKeys`, **`matchesWorkFilter`** —una sola regla para el conteo Y el filtro—, `workBucket`, `workCounts`, `serviceReport`, `scopeLabel`, `scopeProblem`) · default **«Todos los estados»** y `estadoAuto` eliminado · chips y KPIs sobre la **misma lista** (invariante: chip == tarjetas al hacer clic == KPI) · línea «Trabajos hechos: N equipos · E entregados · T en taller · A devueltos/cancelados» + «Contando: …» para poder decir el número con seguridad · chip **«Sin trabajo anotado»** para que los números cierren · estado vacío que **explica** y ofrece el arreglo · **un solo «Limpiar filtros»** (antes «Limpiar» borraba solo las fechas) y «Todo el historial» en vez del ambiguo «Todo» · una sola carga inicial (sin el rebote del montaje).

**Verificación:** `node tools/service_report_test.ts` **68/68** (con el **control negativo** de la regla vieja y el invariante recorriendo todos los chips) · **EN VIVO `tools/verify_trabajos_hechos.mjs` 26/26 sobre DOS copias** (`registro.db` y `dev_registro.db`; crea 3 órdenes de prueba con monto $0, las borra y comprueba contra la base que **el stock volvió al valor inicial**; aborta sin `REGISTRO_DB` o sin día abierto) · regresiones EN VIVO: `verify_recordatorios` **56/56**, `verify_servicio_cierre` **17/17**, `verify_cola_entregas` **11/11**, `verify_metodos_en_cobros` **14/14**, `verify_wizard_metodos` **18/18**, `verify_tecnico_y_fecha_pago` **41/41** · `npm run build` ✓ · `oxlint` **0 errores**.

**Lección (pruebas en vivo):** clickear una opción de un `Select` de Radix por coordenadas puede fallar cuando el portal queda fuera del área clickeable → el script nuevo **abre con click real y navega con el teclado leyendo `[role="option"][data-highlighted]`** (patrón reutilizable). Y una copia sin **días cerrados** hace fallar los chequeos de F39 de `verify_tecnico_y_fecha_pago`: no es una regresión del código, es el fixture.

---

## F45–F48 — «Sin asignar», modal centrado, pantalla agotada sin bloquear y alta guiada (2026-09-20, MODO DEV)

Cuatro pedidos del dueño en la misma jornada, todos en Servicio Técnico y **sin tocar Rust ni la base**.

**F45 — Técnico «Sin asignar» por defecto.** Al crear ya no se prellena el último técnico (se quitó el `last_technician` de localStorage: el elegido se guarda en la ORDEN); sin técnico la orden se guarda igual y la tarjeta en taller muestra el chip ámbar **«Falta asignar técnico»** (`data-needs-tech`, clickeable → selector rápido de F34), que desaparece al asignarlo. No se muestra en entregadas ni anuladas (regla pura `needsTechnician`).

**F46 — El recordatorio de política es un MODAL CENTRADO.** `PolicyToast.tsx` (tarjeta flotante de sonner) se eliminó: ahora `PolicyModal.tsx` + `policy-queue.ts` muestran el aviso al centro con velo suave, tinte claro por tono, acción a un toque y **«Después»**. Invariantes: **bloquea la pantalla, nunca los datos**; no se apila (dedupe por aviso+orden); no insiste por lo ya resuelto; **Escape solo cierra el modal si no hay otro diálogo encima**.

**F47 — Pantalla agotada: aviso rojo, predeterminado y sin bloquear.** `screenOk` ya no depende de la confirmación: el aviso («Esa pantalla no tiene stock… queda en -1 y el movimiento se marca como faltante») queda **en rojo** con la confirmación marcada por defecto y el guardado/cierre siguen disponibles. El gate que SÍ sigue: **elegir** la pantalla cuando hay opciones. Regla pura nueva `outOfStockChoice`.

**F48 — Alta guiada + colores nuevos.** El **color del equipo es obligatorio** (ficha `falta`, paso «Equipo(s)», guardado con «Elegí el color del equipo»); la ficha agrega **observaciones que no bloquean** («Falta el número de teléfono del cliente», «El equipo todavía no tiene técnico asignado») clickeables y sin entrar en `falta`/`completa`; **«Ir al campo» lleva el FOCO** al control del dato (`data-ficha-target`); y la lista de colores suma **Lila** (bg-violet-400) y **Marrón** (bg-amber-800).

**Verificación:** `node tools/policy_queue_test.ts` **13/13** · `node tools/ficha_test.ts` **75/75** · `node tools/service_guide_test.ts` **44/44** · `npx tsx tools/queue_test.ts` **66/66** · **EN VIVO** `verify_tecnico_sin_asignar.mjs` **30/30** (F45+F46+F48 en un flujo: crea por la UI sin técnico y con color, el modal aparece y se pospone, la señal aparece y desaparece al asignar) y `verify_pantalla_agotada.mjs` **16/16** (F47+F48: aviso rojo con el texto exacto, confirmación marcada, guardar habilitado con la agotada, sin color no avanza; **no guarda nada**: el stock se verifica intacto contra la base) · regresiones EN VIVO: `verify_recordatorios` **65/65** (con los chequeos nuevos del modal), `verify_servicio_cierre` 17/17, `verify_cola_entregas` 13/13, `verify_metodos_en_cobros` 16/16, `verify_wizard_metodos` 18/18, `verify_trabajos_hechos` 26/26, `verify_tecnico_y_fecha_pago` 39/41 (los 2 F39 son ambientales: la copia no tiene días cerrados) · `npm run build` ✓ · `oxlint` **0 errores**.

**Lecciones (pruebas en vivo):** un modal bloqueante cambia el orden de los scripts (hay que responder o posponer antes de seguir) y el `dialogText()` genérico pasa a devolver EL MODAL en vez del comprobante (se lee el último `[role="dialog"]`); **Escape en un helper de test es una trampa** (cerraba el wizard entero y la corrida siguiente veía todo `null`); y los clics por coordenadas dejan de servir cuando el contenido cambia de alto (una línea más en la ficha movió el pie del wizard fuera del área clickeable) → la regla que quedó es **verificar el efecto y reintentar con el clic del propio elemento**.

---

## F49 — El «Cerrar» de la tarjeta se vuelve «Descuento» (y sale en la factura) (2026-09-20, MODO DEV)

**Pedido del dueño:** «el cliente me pide que quite el botón de cerrar que sale en la card de servicios, y lo cambie por un botón de descuento para hacerle en ese servicio, y que se refleje en la factura que se le aplicó un descuento de X monto».

**Qué se hizo:**
- **La tarjeta cambió el botón:** fuera **«Cerrar»** (asistente de cierre) y en su lugar **«Descuento»** (`data-discount="<id>"`, con icono de porcentaje). El **asistente de cierre sigue disponible** por el camino del mostrador: el botón **«Cerrar entrega»** de la barra (o F4) que abre la cola → elegís la orden → se abre el asistente. El botón «Descuento» no aparece en órdenes anuladas (Devuelto/Cancelado): ahí no hay nada que cobrar.
- **Diálogo de descuento (`DiscountDialog.tsx`):** abre con el descuento **actual** de la orden, muestra el **precio de lista**, el **descuento** y el **total** en vivo, chips rápidos ($1/$2/$3/$5/$10) + **«Sin descuento»**, y guarda con la **ÚNICA vía de actualización de órdenes** (`updateOrderKeepingFields`) → no pisa ningún otro campo. Si ya hay abonos, avisa en vivo cómo queda el saldo («Ya hay $X cobrado… va a quedar un saldo de $Y / la orden queda saldada»). **No toca la caja**: no crea ni borra pagos.
- **Regla pura `src/lib/discount.ts`** (con `tools/discount_test.ts` **21/21**): el invariante del proyecto es `amount` = lo que el cliente DEBE y `discount_amount` = el descuento → **el precio de lista es la suma**, y un descuento nuevo se calcula **desde ese precio** (nunca sobre el total ya descontado, que se descontaría dos veces); se acota a `[0, precio]` (nunca plata inventada) y `discountProblem` avisa sin bloquear.
- **LA FACTURA (el pedido explícito):** cuando la orden tiene descuento, el comprobante imprime **`PRECIO`** (de lista), **`DESCUENTO`** (en negativo) y **`TOTAL`** (lo que paga el cliente) — en el recibo principal **y en el talón CORTA TIJERA**. Sin descuento la factura **no cambia** (sigue con un solo `TOTAL`, sin líneas en $0.00 que confundan al cliente). El ancho del papel sigue respetado (32/48 chars).

**Pruebas:** `node tools/discount_test.ts` **21/21** · `node tools/receipt_acuerdo_test.ts` **74/74** (era 52: se suman los casos de descuento — con y sin descuento, montos exactos, cortesía del 100% y sin desbordes en 58/80 mm) · **EN VIVO** `node tools/verify_descuento.mjs` **15/15** (la tarjeta sin «Cerrar» y con «Descuento»; el diálogo abre con el descuento actual; aplicar $5 guarda `amount=25 / discount_amount=5` **verificado por IPC**; el chip y el total nuevo en la tarjeta; **la factura imprime PRECIO $30.00 · DESCUENTO -$5.00 · TOTAL $25.00**; quitar el descuento devuelve el total a $30; borra su orden) · regresiones EN VIVO actualizadas al botón nuevo: `verify_servicio_cierre` **18/18** (abre el asistente por la cola + comprueba el botón nuevo), `verify_metodos_en_cobros` **16/16**, `verify_recordatorios` **65/65**, `verify_cola_entregas` 11/11, `verify_tecnico_sin_asignar` 30/30, `verify_trabajos_hechos` 26/26 · `npm run build` ✓ · `oxlint` 0 errores.

**Lecciones:** (1) un cambio de botón en la tarjeta **rompe las pruebas que lo clickeaban** → los scripts ahora abren el asistente por la COLA (`verify_servicio_cierre`, `verify_recordatorios`, `verify_smoke_integral`); (2) el descuento se guarda **siempre** desde el precio de lista (sumar el descuento al total guardado), que es lo que impide descontar dos veces; (3) la factura solo agrega las líneas cuando hay descuento: el papel de las órdenes sin descuento queda idéntico.

---

## F51 — Inventario: ORDEN POR COLUMNAS en Productos (2026-09-20, MODO DEV)

**Pedido del dueño:** «me pide que también en Producto tenga el ordenamiento por columnas».

**Qué se hizo:** clic en el **encabezado** de una columna para ordenar, con ciclo **asc → desc → sin orden**, flecha visible y `aria-sort` (`data-sort` / `data-sort-state` para las pruebas); la paginación vuelve a la página 1 al cambiar de columna. Server-side (`get_products_page`), con **las dos claves de cada columna**: nombre, categoría, marca, modelo, **variante**, **precio**, **costo**, stock, mínimo (y se conservan las claves viejas del desplegable). Componente `SortHead` en `ProductsTab`.

**Dos defectos reales que salieron al verificar (arreglados):**
1. **Faltaba la clave inversa** de varias columnas (`costo_desc`, `precio_desc`, `minimo_desc`): el ciclo del encabezado las mandaba, el `match` de Rust no las tenía, caía en el `_ => "p.name"` y **el segundo clic no hacía nada** (la tabla quedaba igual). Ahora están todas, con un **test Rust nuevo** (`test_product_sort_keys_are_valid`) que ejecuta las 23 claves contra SQLite y comprueba que además ORDENAN (caro→barato y variante).
2. **Las columnas de Precio/Costo desaparecían** según lo que hubiera en la página (`anyPrice` se calculaba sobre `items`): aparecían y desaparecían al cambiar de filtro, de página **o de orden**, y con ellas desaparecía el encabezado para ordenar por precio. Ahora **se muestran siempre** (un producto sin precio lo dice con su chip y el costo va en «—»).

**Verificación (EN VIVO, `tools/verify_orden_columnas.mjs`, 12/12, solo lectura):** cada encabezado existe y es un botón; al hacer clic **el orden de la pantalla se compara con el MISMO orden calculado sobre la BASE leída aparte** (conexión `node:sqlite` de solo lectura) — comparar la pantalla consigo misma no probaría nada; el ciclo asc→desc→sin orden con su `aria-sort`; la paginación vuelve a 1. Además: `node --check` de los scripts, `npm run build` ✓, `oxlint` **0 errores** y **`cargo test --lib` 134/134** (8 ignorados).

**Lección:** un `match` con claves de orden que caen en un `_` por defecto **falla en silencio** (el clic no hace nada y no hay error). La prueba en vivo lo cazó comparando contra la base, y el test Rust lo fija para siempre.

## F50 — Inventario: «lo que uso» (el check) + códigos de referencia (2026-09-20, MODO DEV)

**Pedido del dueño:** «muchos modelos está bien, pero él no lo usa todo; aplicar un check **con su número** de cada producto o modelo que él pueda seleccionar: ésos son los productos o modelos que le van a aparecer cuando está haciendo un registro de un nuevo servicio… así es más rápida la búsqueda». El check va **en los dos** (producto y modelo) y **el inventario tiene que seguir descontando por servicio**.

**Qué se hizo:** columnas nuevas (AL FINAL del orden físico) `products.in_use` / `products.code` y `phones.in_use` / `phones.code` / `phones.default_product_id`; **seed** una sola vez «en uso ⇔ tiene stock» + códigos `P-0001` / `M-0001`; comandos **angostos** `set_product_in_use` / `set_phone_in_use` / `set_phone_use_all` (transaccional: el teléfono y sus repuestos) / `set_product_code` / `set_phone_code` / `set_phone_default_product`; **Inventario → Productos** con columna «En uso» ordenable, filtros «Solo lo que uso» / «Lo que NO uso» y búsqueda **por código**; **Inventario → Modelos** con el código `M-…` y el check por fila; y el **formulario de servicio** ofreciendo **solo lo que está en uso**, con el interruptor «Ver todos» (persistido) y el aviso «sin usar».

**Tres defectos reales cazados al verificar (arreglados):**
1. **Migración a medias:** el bloque de `products` corre **antes** del `CREATE TABLE phones`; el `ALTER TABLE phones …` iba en el mismo batch guardado por un `SELECT … FROM products` → en una base nueva la tabla no existía, el batch **fallaba en silencio** y la primera consulta del padrón moría con «no such column: code». Ahora **cada tabla se migra donde su tabla ya existe** y **cada columna se prueba por separado**.
2. **El padrón no traía los campos nuevos:** `PhoneListRow` no incluía `in_use`/`code`/`default_product_id` (el frontend los pedía y el backend no los mandaba) → la pestaña Modelos mostraba **todo apagado y sin códigos**. Se agregaron al final de la lista explícita de columnas.
3. **El test del seed probaba otra cosa:** crear el catálogo después de `Database::new` no ejercita el seed de la migración → ahora se llama a la **misma función** que corre `init()` (`phones::seed_phone_in_use`) sobre una base con catálogo ya cargado.

**Invariante del dueño:** test `test_in_use_never_stops_the_inventory_deduction` — una pantalla **apagada a mano** que igual se elige y se entrega **se descuenta** y al reabrir vuelve a la **MISMA** ficha. `in_use` decide qué se OFRECE, nunca qué se descuenta. **Decisión revisada frente a la spec:** la lista de **pantallas** de un modelo **no** se filtra por «en uso» (solo el modelo): apagar una ficha no puede dejar al taller sin poder elegir el repuesto que tiene que instalar.

**Verificación:** `cargo test --lib` **136/136** · EN VIVO `tools/verify_uso_modelos.mjs` **35/35** (contra la base leída aparte; escribe solo los checks y los restaura) · regresiones EN VIVO `verify_orden_columnas` 12/12, `verify_trabajos_hechos` 26/26, `verify_smoke_integral` 110 comprobaciones · `tsc -b` 0 · `oxlint` 0 errores · `harness_security` PASS · `harness_truth` PASS.

## F54 — El aviso de política no tapa la factura (y se quita con un toque) (2026-09-20, MODO DEV)

**Pedido del dueño:** «al finalizar el mensaje que sale de tlf y otro mensaje **no me deja ver la factura la orden**; le doy clic al mensaje y **no se quita**».

**Diagnóstico (medido en vivo):** al entregar con «Imprimir la orden al cerrar», el asistente de cierre encola los avisos de política **y abre la factura** en el mismo paso; el modal centrado (F46) traía un velo `z-[100]` **por encima** de los diálogos (`z-50`), así que tapaba la factura y el **primer clic** lo consumía el velo (con un aviso pendiente, `elementFromPoint` sobre «Cerrar entrega» devolvía `DIV[policy-overlay]`). Y en la tarjeta solo cerraban los botones, el velo o Escape.

**Arreglo:** (1) el aviso **no se dibuja mientras hay un diálogo a la vista** (queda en la cola y sale al cerrarlo: no se pierde ninguno) y el velo pasa a **`z-40`**, por debajo de los diálogos; (2) **un toque en cualquier parte de la tarjeta lo cierra** (equivale a «Después») más una **✕ visible**; (3) el trabajo ya guardado no cambia: nada de plata ni stock.

**Verificación:** EN VIVO `tools/verify_aviso_no_tapa.mjs` **14/14** (entrega real sin cobro y limpieza: la factura se ve sin aviso encima, el clic cae dentro de la factura, al cerrarla el aviso vuelve, tocar el mensaje lo quita, la ✕ cierra el de una segunda entrega, sin residuos) · regresión `verify_recordatorios.mjs` **67/67** (los dos chequeos que exigían la conducta vieja se reescribieron a la nueva).

**Lección:** un modal «bloqueante» dibujado **encima** de otro diálogo hace creer que la app «no responde» (el primer clic se lo come el velo). Cuando dos cosas compiten por la pantalla, **la que informa espera** a la que el operario está usando.

## F52 — Inventario: vista «Por modelo» (una fila por teléfono, con sus variantes adentro) (2026-09-20, MODO DEV)

**Pedido del dueño:** «cuando yo busco un Samsung A70 me sale también A705 — ya este viene siendo otro modelo de tlf; debería salir una sola por modelo… y esos mismos modelos tienen que tener referencia: qué pantalla va a seleccionar para ese modelo». Y: «existen varios duplicados de modelos».

**Qué se hizo:** conmutador de vista en **Inventario → Productos** («Lista (ficha por ficha)» | «Por modelo (una fila por teléfono)»). La vista nueva (`ProductsByModel.tsx`) muestra **una fila por TELÉFONO del padrón** con su código `M-…`, sus **variantes como chips adentro**, cantidad de repuestos, **stock total**, **rango de precios**, su check **«lo uso»** y su **pantalla de referencia**; al desplegarla se ven sus repuestos con **código, variante, precio, costo, stock** y un botón para **fijar/quitar la referencia** del modelo. En la lista plana la **variante pasó a ser columna ordenable + filtro por familia** (OLED incluye «OLED Con Marco»).

**Backend (una sola fuente de cada regla):** `merged_stats` devuelve un struct `PhoneStats` (stock, categorías, **variantes en orden canónico**, **rango de precios**) calculado sobre la MISMA unión clave+alias que el stock; el índice del padrón ahora guarda variante y precio por id (sin consultas extra); `PhoneListRow` gana `variants`/`price_min`/`price_max`/`default_product_name`/`default_product_code` (LEFT JOIN, sin N+1); `get_products_page` gana el parámetro `variant_family` y la constante **`VARIANT_FAMILY_SQL`** (familia = primera palabra del texto recortado) con **paridad Rust↔SQL fijada por test**; comando nuevo **`get_variant_families()`** que llena el filtro con la MISMA expresión SQL. Reglas canónicas en `catalog.rs`: `variant_family`, `variant_is_family`, `variant_rank`, `sort_variants`.

**Bug real cazado por la verificación en vivo:** el SELECT de `get_phone_detail` **no incluía las columnas nuevas** (`supplier`, `in_use`, `code`): `r.get(15..17)` caía en los `unwrap_or` y el despliegue mostraba **todos los repuestos «en uso» y sin código**. Arreglado (lista explícita completa) y fijado con un test que apaga un repuesto y comprueba que la ficha lo dice.

**Verificación:** `cargo test --lib` **139/139** · `node tools/variant_test.ts` **22/22** · EN VIVO `tools/verify_por_modelo.mjs` **27/27** (una sola fila con los chips y los números contra `node:sqlite`; el check del modelo que solo cambia `in_use`; el despliegue con código/variante/precio/costo/stock reales; la referencia fijada y devuelta; el filtro «OLED» = 161 fichas como la base) · regresiones EN VIVO: `verify_orden_columnas` 12/12, `verify_uso_modelos` 35/35, `verify_trabajos_hechos` 26/26, `verify_aviso_no_tapa` 14/14, `verify_smoke_integral` **110/110** · `tsc -b` 0 · `oxlint` 0 errores · `harness_security` PASS.

**Lecciones (pruebas en vivo):** (1) elegir el sujeto de prueba **con el propio backend** (la unión exacta clave+alias), no con un `LIKE` aproximado: la primera versión eligió un modelo con 18 fichas por LIKE y el padrón solo le reconocía 1; (2) el pie del wizard **se mueve** cuando desaparece la línea «Falta: …» → el clic de «Siguiente» tiene que ser **por DOM**, no por coordenadas (fallaba 1 de cada N sin que nada estuviera mal); (3) un script que no cierre el **aviso de política** falla el paso siguiente culpando a la pantalla, porque el primer clic lo come el velo (el smoke ahora lo cierra antes de pulsar «Cerrar entrega»).

## F53 — Un solo nombre por modelo, pantalla de referencia y duplicados (2026-09-20, MODO DEV)

**Pedido del dueño:** «cuando yo busco un Samsung A70 me sale también A705 — ya este viene siendo otro modelo de tlf; debería salir una sola por modelo… esos mismos modelos tienen que tener referencia: qué pantalla va a seleccionar para ese modelo» + «existen varios duplicados».

**Qué se hizo:** (A) **split** de las entradas compuestas en teléfonos reales (`catalog::split_model_models`: parte con 2+ códigos —token con letras y dígitos, sin 4G/5G/LTE—; «K20 Plus MP260» → «K20 Plus» + «MP260»), aplicado dentro de `compat_phones_raw` para que padrón, índice, ficha y cargador vean lo mismo; (B) **migración guiada con vista previa** en Ajustes (`preview_phone_split` ejecuta el rebuild en una transacción que se revierte → el informe es exacto; `apply_phone_split` respalda, extrae la variante del texto con `variant_in_text` —palabra completa: «Camon» no es AM—, reconstruye, numera `M-…` y marca «en uso» los NUEVOS, idempotente y ~4 s); (C) la **pantalla de referencia** del modelo se auto-selecciona en el servicio (`autoScreen(options, referenciaId)`), con `default_product_id` en el combobox y búsqueda de la referencia en modo edición; (D) **asistente de modelos repetidos** por repuestos compartidos, con vista previa por grupo y fusión que conserva los nombres viejos como alias.

**Verificación:** `cargo test --lib` **143/143** (split con casos reales, preview sin escribir, aplicar con A70+A705 numerados y en uso, compatibilidad del producto intacta, stock/precio/movimientos sin mover, idempotencia, «Camon») · `queue_test.ts` **72/72** (autoScreen con referencia) · EN VIVO `tools/verify_modelos_f53.mjs` **20/20** · auditoría `audit_inventory.mjs` antes/después (unidades 703 = 703) · `tsc -b` 0 · `oxlint` 0 errores · `harness_security` y `harness_truth` PASS.

**Lo que costó (para no repetirlo):** (1) leer la base con una conexión `node:sqlite` de larga vida sobre WAL daba una foto vieja y hacía creer que la app no había escrito: **abrir y cerrar la conexión en cada consulta**; (2) la operación tarda segundos y SQLite deja ver las filas a medida que inserta: **esperar el estado FINAL** (sin nombres pegados, sin filas sin código), no «cambió el conteo»; (3) WebView2 **reutiliza el proceso del navegador**: si queda uno vivo, la app nueva arranca SIN el puerto de depuración → hay que cerrar `registro.exe` y `msedgewebview2.exe` antes de lanzar; (4) el comando exige ser el DUEÑO: sin PIN la app lo rechaza (correcto).

## F55 — La auditoría cuenta los mismos teléfonos que el padrón (y su espejo queda fijado por test) (2026-09-20/21, MODO DEV)

**Origen:** el «pendiente anotado» que dejó F53. `node tools/audit_inventory.mjs` contaba los teléfonos con su PROPIA extracción de compatibilidad, sin partir las entradas compuestas: después del split de F53 el reporte decía **1267** teléfonos distintos mientras el padrón de la app tenía otro número. Dos cifras que se leen como si una estuviera mal, cuando la diferencia era que una aplicaba la regla nueva y la otra no.

**Qué se hizo:** se espejó `catalog::split_model_models` en el script (`isModelCode` + `splitModelModels`: un código es un token con **letras Y dígitos**, sin `4G/5G/LTE` ni números puros; con 2+ códigos cada parte es `[familia] + [código] + [palabras hasta el próximo código]`) y se aplicó dentro de `normalizeFields`, que es donde el script arma los teléfonos del catálogo. El resumen ahora **dice cuál número es cuál**: «teléfonos distintos en el catálogo **(script)**» junto a «teléfonos en el **padrón (app)**» (leído de la tabla `phones` solo si la copia auditada la tiene).

**Lo importante — la paridad dejó de ser una promesa:** el script **no puede llamar a Rust**, así que su split es una copia a mano y se puede separar sin que nadie lo note (el mismo riesgo que ya cubrían `canonical_brands.json` + `canonical_fixtures.json`). Se agregó **`tools/split_fixtures.json`** — **17 casos** generados con `node tools/audit_inventory.mjs --gen-split-fixtures` (los reales `A70 A705`, `Galaxy A70 A705`, `A16 4G A165`, `A13 4G A135 M13`, `K20 Plus MP260`, `A01 Core A013`, `Y6 2019 8A`, `K42 K52`; el **mismo código dos veces** —`A70 A70` → se devuelve tal cual, no hay 2 partes distintas—; y los que **NO** se parten: `Redmi Note 11`, `A06 4G`, `Galaxy S21 Ultra 5G`, `Redmi 9A`, `iPhone 11 Pro Max`, `5001 1V 2019`, `Note 20`, vacío) y el test **`catalog::tests::test_split_model_models_match_node_fixtures`**, que corre `split_model_models` contra el JSON y **falla a propósito** si las dos copias divergen. El encabezado del script lo advierte: si se toca `splitModelModels`, hay que regenerar el fixture.

**Residual DOCUMENTADO (no es un bug):** las dos cifras siguen cerca pero no idénticas (script **1267** · padrón **1135** en la base de trabajo) porque (a) el script no aplica el filtro de familia/marca ni los «junk» del backend, y (b) **el padrón solo cambia cuando el dueño aplica la migración de F53** desde Inventario → Ajustes: el `registro.db` de trabajo todavía **no** la tiene aplicada (sus 1135 teléfonos siguen con los nombres compuestos; la migración se verificó sobre copias, con respaldos `backup/registro_pre_modelos_*.db`). El número de NEGOCIO coincide exacto: productos **1087**, unidades totales **703**.

**Verificación:** `cargo test --lib` **144/144** (8 ignorados; +1 = el test de paridad nuevo) · `node tools/audit_inventory.mjs` corre y muestra las dos cifras · `--gen-split-fixtures` regenera el fixture sin diffs inesperados · `tsc -b` 0 · `oxlint` 0 errores (94 warnings preexistentes).

**Lección:** cuando una regla se **copia** a otro lenguaje porque ese lado no puede llamarla (script de node vs backend Rust), la copia tiene que venir con un **fixture + un test que falle al divergir**, no con un comentario que pida mantenerlas iguales. Un reporte con su propia copia silenciosa de una regla es una fuente de verdad enemiga: dice un número distinto y nadie sabe cuál creer.

## ENTREGA — verificación de producción del inventario + instalador local 0.4.2 (2026-09-21)

**Pedido del dueño:** «necesito entregarla, que funcione su inventario, que haga su descuento al seleccionar una pantalla en un servicio… prueba si está funcionando primero». Decisión suya: **instalador LOCAL 0.4.2 sin publicar** (los clientes no se tocan) y **no** meterse con la caja (F40 queda fuera del alcance).

**Punto de partida (lo primero que apareció):** el instalador que había en `instaladores/` era el **0.4.1 del 20/9 15:50**, o sea **ANTERIOR a F44–F55** — el `Registro.exe` y el setup NO traían ni la vista «Por modelo», ni «lo que uso», ni el orden por columnas. Se compiló con el código de hoy y se probó **contra una COPIA de la base del taller** (`backup/prod_final.db`: 1087 productos, 1135 teléfonos con nombres pegados, **sin** las columnas nuevas, 4 órdenes — exactamente el estado del local).

**Lo que se midió (todo sobre la base real copiada, app 0.4.2 recién compilada):**

| Qué | Resultado |
|---|---|
| **El descuento al entregar con la pantalla elegida** (`verify_servicio_cierre`) | **18/18** — «la pantalla elegida y el stock descontado en 1 → **7 → 6**» + movimiento «Servicio Entregado»; y al borrar la orden de prueba el stock **vuelve a 7** |
| Migración de la actualización (`verify_migracion_datos`) | **50/50** — la historia del cliente queda **fila por fila y columna por columna** intacta (ventas, pagos, clientes, stock y precios por id, cierres), se agregan las columnas nuevas y la app nueva lee la base migrada |
| «Lo que uso» + códigos (`verify_uso_modelos`, F50) | **37/37** |
| Selector de pantalla con su stock (`verify_service_screen_stock`) | **6/6** — «stock 7 · 1 agotada» |
| Gate de marca (que no se instale la pantalla de OTRA marca) | **23/23** |
| Pantalla agotada sin bloquear (`verify_pantalla_agotada`) | **16/16** (stock intacto) |
| Inventario rápido + «Por modelo» + orden por columnas | **10/10 · 27/27 · 12/12** |
| «Trabajos hechos», Descuento/factura, Aviso que no tapa | **26/26 · 15/15 · 14/14** |
| Smoke integral (servicios + ventas + libro + ayuda + borrados) | **110/110** |
| `cargo test --lib` | **144/144** (8 manuales ignorados) |

**Lo que NO era culpa del producto (4 falsos negativos, arreglados en las pruebas):** la batería en vivo se cayó al principio con fallos que parecían de inventario. Se investigó **con sondas dirigidas antes de tocar código** y el producto estaba bien en los cuatro casos: (1) la **barra lateral colapsada** deja los botones sin texto y toda navegación por nombre de ítem falla; (2) `typeText` manda las teclas al elemento **enfocado**, y si el clic cae mientras el diálogo se anima el texto se pierde → la prueba fallaba dos pasos después («no se llega al paso 2», «opciones=[]»); (3) dos scripts eran **viejos**: buscaban las opciones del selector con `role="option"` (hoy usan `data-model-option`) y pulsaban «Cambio pantalla» para abrir el selector cuando al CREAR ese trabajo **ya viene elegido** (o sea lo apagaban); (4) `verify_uso_modelos` no fijaba la preferencia «Ver todos», que **se recuerda a propósito**, así que una corrida anterior la dejaba encendida y el arranque ya no era «solo lo que uso». Se agregó al driver `ensureSidebarExpanded()` (se corre al importar) y `escribirEn()` (enfoca, escribe y **comprueba el valor**), y la prueba de F50 devuelve la preferencia como estaba (no le cambia la configuración al local).

**Lo que quedó pendiente, dicho sin adornos:**
- **El padrón de la base REAL todavía tiene 124 nombres pegados** («Galaxy A70 A705», «A13 4G A135 M13»…). El split ya está en la app: se aplica solo cuando el padrón se rearma (al agregar/editar un producto) o con el botón de **Inventario → Ajustes** (con vista previa y respaldo). En la copia de prueba quedó en **0 pegados y 1201 teléfonos numerados**, verificado. No es un bloqueante: el taller puede trabajar igual y separarlos cuando quiera.
- **123 fichas con stock (333 unidades) sin precio** — son modelos más nuevos que la lista de precios del local; el dueño ya aceptó esa excepción el 18/9 (se cargan desde Inventario → Precios y datos). **Esas no se pueden cobrar hasta que tengan precio.**
- Las features 37 (métodos en Bs por whitelist) y 40 (libro único de movimientos de caja + auditoría) siguen abiertas por decisión del dueño.

**Instalador:** `instaladores\Registro Servicio Tecnico_0.4.2_x64-setup.exe` (6,5 MB, firmado, **NO publicado**: ni release en GitHub ni manifiesto del updater → las PCs del local siguen en su versión hasta que se corra ese setup a mano). Gate de release: **0 bloqueantes**, 8 comprobaciones OK y 5 avisos (los 5 ya conocidos, incluida la excepción aceptada de precios).

**Lección (pruebas en vivo):** un fallo de la batería **no es** un fallo del producto hasta que se demuestra. Los cuatro de esta sesión se resolvieron mirando el DOM real con una sonda dirigida — y en los cuatro el producto estaba bien. La batería ahora **dice qué falló y con qué valor** (foco, valor escrito, `data-model-all`, nombres pegados en la base) en lugar de morir con «click target no encontrado».



---

## F65 — Categorías de PRODUCTO: crear, corregir y borrar (2026-09-23) — MODO DEV

**Pedido del dueño:** «necesito que cuando este inventario pueda registrar nuevas categorías, no esté limitado a crear categorías de productos». La categoría era una lista CERRADA (las 6 del `init` + lo que trajeran los catálogos importados): el repuesto que no entraba en ninguna se anotaba en la menos mala. Alcance acordado con el dueño: **crear** (cualquiera que registre productos) + **gestionar** —corregir y eliminar— en la pestaña Ajustes, que ya es solo-dueño.

**Qué se hizo.** `add_category`/`rename_category`/`delete_category` (+ la lectura `get_categories_with_usage`) con TODA la validación fail-closed en el backend y las tres escrituras en el gate del DUEÑO (lista del test B3). `add_category` NO crea gemelas: si el nombre ya está (plegado: mayúsculas, acentos y espacios no cuentan) devuelve la existente con `created: false` y la UI ofrece «Usar «Pantalla»». `rename` no toca ninguna ficha (mismo `category_id`; el nombre nuevo se ve por JOIN) y `delete` solo borra categorías VACÍAS (con productos dice cuántos son). El padrón de teléfonos (`PHONE_CATEGORIES`) queda fijo. Frontend: `NewCategoryInline` («+ Nueva categoría» dentro del formulario del producto) y `CategoriesCard` (tarjeta «Categorías del catálogo» en Ajustes con el uso real), reglas puras en `src/lib/product-categories.ts` y la caché de categorías del `db.ts` invalidada en cada escritura (antes una categoría creada no aparecía en el filtro hasta reiniciar).

**2ª vuelta adversarial (2 revisores independientes, uno de calidad/UX y otro de consistencia/regresiones).**
- **(BLOQUEANTE) Escape con el panel abierto cerraba el formulario del producto ENTERO** — medido en vivo con una sonda: se perdían los ~12 campos cargados. Radix escucha Escape en la captura de `document` (registrado al montar el diálogo) y el `onKeyDown` del input llega tarde. Se resolvió con `useEscapeGuard` (captura de `window`, que va antes) y se aplicó **también al «+ Nueva categoría» de Servicios (F62)**, que tenía el mismo defecto latente: ahí se perdía la ORDEN que se estaba registrando.
- **(MAYOR) El padrón se definía por ID en el motor y por nombre en F65, y `AUTOINCREMENT` repartía los ids del padrón:** en una base creada por `init()` la **12.ª y la 13.ª** categoría del dueño caían en los ids 18 y 19 = `PHONE_CATEGORIES` → sus fichas entraban al padrón de Modelos, el barrido «la lista es todo» les ponía el stock en 0 y quedaban fijas de por vida. Ahora el padrón se protege **solo por ID** (como el motor) y `add_category` **reserva** esos ids con `id` explícito (`MAX(id)+1` salteando 1/18/19); el test crea 14 categorías y exige que ninguna caiga en 18/19.
- **(MAYOR) El guardado del producto fallaba MUDO:** el invoke rechazado por el gate de dueño dejaba el diálogo abierto sin ningún mensaje (la cajera creía la ficha cargada y la volvía a cargar → duplicado). Ahora hay try/catch con toast, aviso si falta el nombre y toast de éxito.
- **(MAYOR) El producto nuevo nacía en «Accesorio»** (primera alfabética) mientras la pestaña está filtrada en «Pantalla»: parecía que no se había guardado. Ahora arranca en la categoría del filtro activo.
- **12 detalles más, todos corregidos:** plegado del frontend idéntico al backend (paridad probada), tope contado en caracteres reales, nombre sin ninguna letra rechazado (plegaba a clave vacía), el error rojo no aparece al abrir el panel, «Usar …» solo donde sirve, el uso real se relee cuando el inventario cambia, el error de lectura se dice con «Volver a leer» (antes devolvía `[]` y parecía que habían desaparecido las categorías), `maxLength` en nombre y descripción, el foco vuelve al «+», sin toast de cambios inexistentes, `partLabel` recorta el prefijo de la categoría REAL, y la migración idempotente de `categories.description` para bases instaladas viejas.

**Verificación:** `cd src-tauri && cargo test --lib` → **146 passed / 0 failed** (8 ignorados) · `node tools/category_rules_test.ts` **42/42** · `node tools/verify_categorias_producto.mjs` **35/35 EN VIVO** (crea, guarda, elige, evita la gemela en dos casos, Escape que no cierra el formulario, filtro al instante, Ajustes, corrige, bloqueos del padrón y de «tiene productos», elimina y **limpia: mismas categorías con los mismos id, nombre y descripción + los mismos productos**) · `node tools/verify_categoria_nueva.mjs` (regresión F62) **12/12** · `npm run build` (tsc + Vite) ✓ · `oxlint` 0 errores (98 warnings preexistentes en `tools/governance`) · `harness_security` **PASS** · `harness_truth` **PASS** · `npx tsx tools/cli/index.ts parallel` security ✅ review ✅ build ✅.

**Herramientas del harness (estado, para la próxima sesión):** `harness_spec`/`harness_security`/`harness_truth` funcionan; **`harness_review` sigue roto** en esta copia (falta `tools/reviewer/parallel-review.ts`) → se usaron **dos subagentes revisores adversariales** (calidad y consistencia) con informes por severidad, como en sesiones anteriores. **`harness_close` marcó «deploy readiness: FAIL»** — esperado y justificado: MODO DEV, sin release ni push y app de escritorio sin destino de despliegue; tests/build/truth pasaron. OJO: `harness_close` **no escribió** en esta copia (`tools/progress/current.md`, `history.md` y `feature_list.json` quedaron intactos: las marcas de tiempo lo confirman) → el cierre se hizo a mano (status `done` en `feature_list.json` + esta entrada).

**Lecciones:** (1) **Escape y Radix**: cualquier panel propio dentro de un `Dialog` necesita `useEscapeGuard` — el `onKeyDown` del input no alcanza y una tecla puede costar el formulario entero; (2) **un contador de ids AUTOINCREMENT y una lista de ids con significado (`PHONE_CATEGORIES`) son una trampa**: si algo se identifica por posición numérica, quien crea filas tiene que reservar esos ids; (3) **una UI que promete y un backend que decide tienen que plegar IGUAL** (dos implementaciones de lo mismo se prueban por paridad, como `canonical_fixtures.json`); (4) **un `catch` que devuelve una lista vacía convierte un error en «desaparecieron los datos»**: el error se dice.

---

## F66 — Wizard de servicio sin ruido + la pantalla junto al modelo (2026-09-23) — MODO DEV

**Tres pedidos del dueño, con el wizard de recepción abierto en la mano:**

1. «cuando vas a crear un servicio esta información ocupa demasiado espacio del wizard, no debería estar ahí, está muy grande» (la FICHA DE INGRESO: 4 renglones).
2. «hay este botón de descuento grande, ese input no debería estar ahí o si está que uno aplique y coloque un monto X; de todas maneras tengo botón de descuento en la card».
3. «el input debería estar cerca al colocar el modelo, que pueda elegir la pantalla de ese modelo o su compatibilidad, pero con el beneficio de buscar otra pantalla que desee el operador seleccionar».

**Qué se hizo.** (1) La ficha de ingreso es UNA línea — `Ficha de ingreso · 6/16 · Siguiente dato: … [Ir al campo] · → Siguiente en el proceso: … · ⚠N · Ver ficha` — MEDIDO en la ventana real: **157 px → 61 px**; los 4 bloques, los obligatorios que faltan, los avisos que no bloquean y la explicación del paso siguiente quedan adentro de «Ver ficha», y la guía de cada dato en el tooltip. (2) El descuento pasó al lado del precio en los DOS formularios (`Monto [30] − [Desc.]`, 36 px) con el total a la vista solo si hay descuento (`$30.00 − $5.00 = Total $25.00`); las dos líneas de ayuda quedaron en el tooltip y el botón «Descuento» de la tarjeta no se tocó. (3) El bloque de pantalla se movió justo debajo del modelo/monto/color y `ScreenSelect` ganó **búsqueda libre** en las tres categorías de pantalla: la lista sigue siendo la compatibilidad del modelo y abajo se busca cualquier otra; la elegida a mano se marca «buscada», avisa que no figura en la compatibilidad y **descuenta igual** (el gate solo pide que haya una elegida), y se suelta al cambiar de modelo.

**Verificación:** sonda en vivo sobre el wizard real (ficha 157→61 px; descuento en la misma fila que el monto; pantalla debajo del modelo; búsqueda «Redmi 9A» → 2 resultados → elegida + aviso + «Siguiente» habilitado con modelo+color+pantalla) y regresiones EN VIVO: `verify_wizard_metodos` **18/18**, `verify_compat_pantalla` **12/12**, `verify_tecnico_sin_asignar` **32/32**, `verify_pantalla_agotada` **16/16**, `verify_service_screen_stock` **6/6**, `verify_descuento` **15/15**, `verify_recordatorios` **67/67**, `verify_smoke_integral` **110/110** · `npm run build` ✓ · `oxlint` 0 errores.

**Dos lecciones (una del producto y una del método).**
- **Del producto:** la ficha NO puede usar la palabra «Falta:». El pie de cada paso del wizard ya la usa para ESE paso y dos pruebas deciden con ella si el paso está completo; la ficha habla de la ficha COMPLETA. Ahora dice «Siguiente dato:». (Lo cazó el smoke: un fallo que parecía de layout y era de texto.)
- **Del método:** **dos scripts de CDP no pueden correr a la vez sobre la misma ventana.** `verify_descuento` y el smoke «fallaron» (2 y 3 checks) cuando los lancé en paralelo — se pisan clics, foco y hasta el conteo de órdenes de prueba (el smoke vio «servicios 7 → 6» porque el otro script creaba y borraba órdenes al mismo tiempo). De a uno: 15/15 y 110/110. **Las verificaciones en vivo van EN SERIE.**

### Anexo F66 — El desplegable del modelo se cierra al elegir (mismo día)

Reporte del dueño: «cuando colocas un modelo, selecciono el modelo, se cierra el desplegable; no tenga que darle dos veces al modelo para cerrarlo».

**Causa raíz medida:** `ModelCombobox.pick()` cerraba la lista y **volvía a enfocar el input** (para que el operario siga tipeando). Ese `focus()` dispara `onFocus → setOpen(true)`: la lista **se reabría en el acto** y había que clickear afuera (o el mismo modelo otra vez) para cerrarla.

**Fix:** el foco programático se marca (`focusSinReabrir`) y `onFocus` ya no reabre (el foco se conserva: se puede seguir escribiendo). **Efecto secundario que cazó una sonda:** con el campo YA enfocado, un clic sobre él no dispara `focus`, así que después de elegir no se podía volver a abrir la lista con el clic → se agregó `onClick={() => setOpen(true)}` (el clic en el campo SIEMPRE muestra la lista). El **chevron** ahora abre y cierra de verdad (antes el `focus` lo dejaba solo abriendo).

**Verificado en vivo:** tras elegir → `[data-model-option]` = 0 · clic en el campo → la lista vuelve · chevron → abre/cierra/abre · el interruptor «Ver todos» sigue funcionando después de elegir (rótulo y `localStorage` acompañan) · `verify_modelo_legible` **13/13** · `verify_smoke_integral` **110/110**.

**Dos cosas que salieron de paso (y valen):**
1. **`verify_uso_modelos` DEPENDÍA del bug**: elegía un modelo y seguía clickeando DENTRO de la lista (que quedaba abierta). Se actualizó para reabrirla (foco en el campo) y para pulsar el interruptor **por DOM** (un clic por coordenadas puede caer en el overlay y cerrar el wizard entero, ya documentado en ese script).
2. **Esa prueba ahora PREPARA SU FIXTURE**: desde F64 el catálogo viaja todo EN USO (`tools/enuso_todo.mjs`), así que no hay nada apagado que esconder y fallaba **6 comprobaciones por falta de datos** (no por el producto; se comprobó corriéndola ANTES de tocar el combobox: 22/28 con los mismos 6). Apaga 1 ficha y 2 modelos al empezar (antes de su foto «antes») y los devuelve al terminar: **22/28 → 37/37 EN VIVO**.

---

## F67 — El precio del repuesto: al elegir la pantalla se TOMA su precio de venta (2026-09-23)

**Pedido del dueño (SERVICIOS):** «quiero que en el servicio cuando yo seleccione una pantalla pueda tomar el precio de venta de ese producto, o se puede seguir usando también el que tengo al lado de modelos» + «que se pueda tomar los precios del producto».

**Diagnóstico:** la lista de pantallas **mostraba** el precio pero elegir una no movía el monto; el único camino de precio (`applyModelPrice`) leía la lista de candidatos del **modelo anterior** (aplicaba precios del modelo que se acababa de dejar) y, en efectivo, dejaba el Total en **2·contado − lista** (lista 28 / contado 25 → el cliente pagaba 22).

**Implementado:** `src/lib/screen-price.ts` (regla pura: `partPrice`, `priceFields`, `groupPriceFields`, `pricePatch`, `amountTypedPatch`, `priceSource`, `sameMoney`) como **única** implementación del precio; precedencia **pantalla > modelo > lo escrito a mano**; el precio automático se aplica en un efecto y **solo con los candidatos del modelo actual** (`useCompatibleProducts` ahora devuelve `alDia`); botones de un toque «Usar precio de la pantalla $P» / «Usar precio del modelo $M»; el rótulo dice de dónde salió el monto; en EDICIÓN nunca se cambia un monto guardado solo. En la base, `amount` es el **total** (lista − descuento) — verificado con una orden de prueba creada, guardada y borrada.

**Revisiones adversariales (2 subagentes): 1 BLOQUEANTE + 3 MAYORES + 10 menores.** El bloqueante: **el descuento calculado del efectivo se aplicaba sobre el precio que el operario había escrito** (Monto 99 → Total 96). Arreglado: el descuento calculado viaja con el monto sugerido y se va cuando él teclea su monto (el descuento **suyo** nunca se toca). Mayores: una aserción vacua del oráculo del modelo, el chequeo del criterio 6 que **no podía fallar** (ahora usa un equipo con el monto sin tocar y exige que elegir una ficha con precio distinto no lo mueva) y una segunda implementación del precio en el formulario (`divHints`) que podía contradecir el rótulo → **eliminada**.

**Verificado:** `node tools/screen_price_test.ts` **45/45** · **EN VIVO `node tools/verify_precio_pantalla.mjs` 52/52** (×2, determinista; incluye el ida y vuelta por la BASE: la orden guarda `amount` = precio contado, `discount_amount` y `screen_product_id`, y después se borra) · regresiones EN VIVO: `verify_wizard_metodos` 18/18, `verify_descuento` 15/15, `verify_compat_pantalla` 12/12, `verify_pantalla_agotada` 16/16, `verify_recordatorios` 67/67, `verify_servicio_cierre` 18/18, `verify_tecnico_y_fecha_pago` 41/41, `verify_uso_modelos` 37/37, `verify_smoke_integral` **110/110** · `cargo test --lib` **146/146** · `tsc -b` 0 · `oxlint` 0 errores · `harness_security` PASS · `harness_truth` PASS.

**Dos lecciones de método (costaron tiempo real):**
1. **El binario de dev sirve el `dist` EMBEBIDO**: después de tocar el frontend hay que `npm run build` **y recompilar** (`cargo build --no-default-features`); recargar la página no alcanza (el `index.html` servido sigue apuntando al bundle viejo — se comprobó pidiéndolo con `fetch(..., {cache:'no-store'})`).
2. **Con la app instalada abierta, `tauri dev` no levanta el puerto de depuración**: las dos instancias comparten la carpeta de WebView2. Receta: `WEBVIEW2_USER_DATA_FOLDER` a una carpeta **fuera del proyecto** (adentro, el watcher de Vite muere con `EBUSY` sobre `Cookies`), `--remote-debugging-port=9222` y `REGISTRO_DB` a una copia.

**Pendiente anotado (no es de F67):** si se cambia de modelo y el nuevo **no tiene ningún precio**, un monto que había escrito la regla queda como estaba (ahora con el aviso «Revisá el monto: este modelo no tiene un precio único»), y la pantalla elegida de otro modelo sigue viajando en `screen_product_id` (comportamiento viejo).

---

## F68 — Sesiones de caja: Master y Caja 1 (2026-09-23)

**Pedido del dueño:** «sería bueno la sesión de caja 1 pueda usar todo, ver su día de caja, pero no pueda ver cuánto factura la master; no tenga tanto acceso». Con **una sola PC** y **sin fotos** por ahora: arranca el Sprint A de `AUDITORIA_ENTREGA.md`.

**Antes:** un PIN compartido y el rol «cajera» era **un botón sin autenticación**; **ningún movimiento de plata tenía autor** (0 columnas `user/_by/who` en la base) y `sync_log` existía sin escribirse nunca; la caja veía el inventario **con precios de costo**.

**Hecho:** tablas **`users`** (rol `master`|`caja`, PIN propio PBKDF2, activo) y **`cash_movements`** (el libro de plata con el **autor** copiado); la sesión dejó de ser un booleano y pasó a ser **quién** está usando la app (con vencimiento de 12 h y compatibilidad con `owner_session_set`/`lock_owner`); el gate `require_owner` ahora exige rol `master`; **el libro se escribe en la misma transacción** de cada movimiento de dinero (venta, abono, contra-asiento al borrar, devolución, gasto, gasto borrado, apertura, cierre, reapertura); 9 comandos nuevos (personas + libro filtrado por sesión + resumen por persona, del dueño); **acceso por persona** (selector + PIN propio, retoma la sesión al recargar, PIN directo cuando hay una sola persona); **«Personas y accesos»** en Libro Diario (solo Master); pestaña **«Movimientos»** con el autor; y el **costo** oculto para la caja. Regla pura en `src/lib/session.ts` (41/41) — fail-closed: un rol desconocido cae en el perfil de CAJA.

**Lecciones:** (1) la sesión vive en el backend 12 h → al recargar hay que **retomarla** o la UI y el backend quedan en dos verdades; (2) **las sondas de un gate no pueden mutar nada si pasan** (usar `set_pin` como sonda le cambió el PIN al Master en la corrida donde la sesión era la suya); (3) `add_sale` devuelve `()` → el IPC llega como `null`, el éxito se comprueba por la fila en la base; (4) los atributos `data-*` van donde se escriben (fila vs celda) — leerlo del lugar equivocado parecía un fallo del producto; (5) **nunca hacer cirugía de archivos con PowerShell**: un `Set-Content` con una variable nula me vació el script de verificación (19556 → 0 bytes) y hubo que reescribirlo.

**Verificado:** `cargo test --lib` **148/148** (2 tests nuevos) · `node tools/session_test.ts` **41/41** · **EN VIVO `node tools/verify_sesiones_caja.mjs` 28/28** (acceso por persona, PIN equivocado, la caja ve todo el mostrador y vende, la caja NO ve los movimientos de otras sesiones, 5 comandos del dueño rechazados por IPC, la venta de la caja en el libro con SU nombre, el Master viendo todo + resumen por persona + la pestaña Movimientos con el autor) · regresiones: `verify_wizard_metodos` 18/18, `verify_pantalla_agotada` 16/16, `verify_compat_pantalla` 12/12, `verify_descuento` 15/15, `verify_smoke_integral` **110/110 (×2)** · `tsc -b` 0 · `oxlint` 0 · build ✓ · `harness_security` PASS · `harness_truth` PASS.

**Sigue (Sprint A):** F69 — que cada caja cuadre su día (el esperado todavía sale de `compute_daily_totals`, que **no** incluye gastos ni el fondo de caja: eso es F69) · F70 — anular una venta · F71 — respaldo/restauración desde la app + los arreglos baratos.

---

## F69 — Cada caja cuadra su día: el arqueo cuenta el fondo y los gastos del cajón (2026-09-23)

**Origen:** el **bloqueante A1 de `AUDITORIA_ENTREGA.md`** (la auditoría hecha para entregar el producto al cliente final) más el tronco de la revisión adversarial de F68. Spec: `tools/progress/specs/F69-arqueo-del-cajon.md`.

**Antes (medido en el código):** `compute_daily_totals` **no mencionaba `expenses` ni `initial_cash` ni una vez** → un día perfecto en el que se pagó algo **del cajón** cerraba «faltando» ese monto y el fondo de caja lo hacía «sobrar» todos los días; el diálogo de cierre **precargaba los digitales con el monto del sistema** (`actual_zelle = expected.zelle_total`, ídem Pago Móvil y Transferencia) así que su diferencia daba **0 SIEMPRE** y el «verificalo en el banco» era decorativo; `expenses` no tenía de dónde salió la plata; y del tronco de F68 quedaban `reverse_book_entry` fechando el contra-asiento con **hoy**, `get_cash_movements` **fail-open** sin sesión, `get_profit_summary`/`get_inventory_value`/`export_data` sin gate, el **costo del catálogo viajando** al frontend, `verify_user_pin` **sin límite de intentos** y `set_pin` pisando el PIN de todos los masters.

**Hecho:** regla pura **`src/lib/drawer.ts`** (desglose = efectivo cobrado + fondo − gastos pagados del cajón, líneas del arqueo, qué falta confirmar, diferencia por moneda con su tolerancia y `esDeCajon`); **`close_day` con la misma fórmula** leyendo los gastos del **libro de plata** (una sola fuente) y columnas nuevas `drawer_adjust_usd/_bs` para que un cierre guardado se explique solo; **las devoluciones NO se restan otra vez** (vienen netas en el método por el que salió la plata); migración `expenses.method` + selector **«¿De dónde salió la plata?»** (un gasto sin declarar no se descuenta y el cierre lo avisa); **el arqueo pide un acto humano por línea** (nada entra «porque el sistema lo dice»: nace «Sin contar», se confirma escribiendo o con «Es el esperado», y **Cerrar Día se rechaza** nombrando lo que falta sin cerrar el día); el diálogo de cierre **se abre con los datos ya leídos** (antes mostraba el desglose en $0.00 mientras cargaba); `closingDifference` suma el ajuste guardado (la lista de Cierres ya no inventa descuadres); **contra-asiento con la fecha del movimiento original**; accesos: `get_cash_movements` fail-closed, utilidad/capital/respaldo del dueño, **costo borrado en el origen** para la sesión de caja, PIN con límite de intentos, master sin PIN imposible con más de una persona, `set_pin` por dueño en sesión, `remove_pin` limpiando también `users.pin_hash`, `ensure_master_user` que no tumba el arranque; y en la UI `abilities()` **usado de verdad** (Gastos/Salud/Personas/Cerrar Día), «Editar»/«Nuevo producto» y el costo de Pedidos escondidos para la caja, y `App.tsx` sin asumir dueño si no pudo leer el padrón.

**Lecciones:** (1) **un número sin contar no es un número** — precargar un conteo con el valor del sistema hace que la diferencia dé 0 y el control sea decorativo; el default se reemplaza por un acto humano y el semáforo no dice «Cuadra» mientras falte; (2) **correr la verificación en vivo sobre DOS copias encontró un bug que una sola no veía**: con el turno abierto del mismo día que el reloj, el contra-asiento fechado «hoy» coincidía por casualidad; en la copia con el turno del 21 y el reloj en el 23 apareció el fantasma (el 21 descontaba para siempre y el 23 recibía un «+» inventado) — **el fixture tiene que poder diferir del reloj**; (3) la verificación se mide contra una **base relativa** (los gastos del cajón que la copia ya tenía), no contra cero, o una copia con historia hace fallar aserciones correctas; (4) un diálogo que decide plata **no puede abrirse vacío**; (5) `data-field` duplicado engaña a la prueba (la diferencia por línea y la del resumen no pueden compartir atributo); (6) **la UI no es la seguridad, pero tampoco puede regalarse**: esconder una columna no cierra la fuga, el número viajaba igual y se leía con un invoke directo.

**Verificado:** `cargo test --lib` **151/151** (3 tests nuevos) · `node tools/arqueo_test.ts` **45/45** · `node tools/pos_cuadre_test.ts` **66/66** · `node tools/session_test.ts` **41/41** · **EN VIVO `node tools/verify_arqueo_f69.mjs` 39/39** (y 37/37 sobre una segunda copia: desglose con fondo y gasto del cajón, esperado igual al del backend, todas las líneas «sin contar», cierre rechazado sin contar con el día **sigue abierto** en la base, un conteo distinto bajando el semáforo de esa moneda, el conteo real devolviendo el cuadre, la caja sin Cerrar Día/Gastos/Salud/Personas, utilidad/capital/respaldo rechazados por IPC y el catálogo sin costo, más la limpieza del gasto con su contra-asiento) · regresiones EN VIVO: `verify_sesiones_caja` **28/28**, `verify_tecnico_y_fecha_pago` **OK** (sobre una copia **con día cerrado**), `verify_smoke_integral` **OK** · `tsc -b` 0 · `oxlint` 0 errores · build ✓ · `release_gate` **LISTO** (5 avisos aceptados) · `harness_security` PASS · `harness_truth` PASS.

**Sigue (Sprint A):** F70 — anular/corregir una venta con reverso de stock · F71 — respaldo/restauración desde la app + los arreglos baratos.

### F69 — 2ª vuelta adversarial (3 subagentes): 4 BLOQUEANTES + 13 MAYORES, todos arreglados

Los revisores (plata, seguridad/permisos, consistencia/veracidad) encontraron, y se arregló:

- **[B] Un día SIN MOVIMIENTOS no se podía cerrar.** `getDailyTotals` sólo devuelve días con ventas/abonos/entregas, así que un día de sólo recepciones (o feriado) llegaba como lista vacía → `expected = null` → botón apagado con el aviso rojo, y `open_day` se negaba a abrir el día siguiente: turno abierto para siempre. Ahora lista vacía = día en ceros (como hace el backend) y `null` queda sólo para un fallo real de lectura.
- **[B] Un día con el PUNTO EN NEGATIVO no se podía cerrar** (una devolución anotada por Punto, el caso real de F42): la regla exigía confirmar la línea y la pantalla la dibujaba sólo con `> 0`. Ahora manda la MISMA condición que la regla.
- **[B] `open_day` REABRÍA un día ya cerrado** (el `ON CONFLICT … is_closed=0`) reescribiendo fondo y tasa y borrando la apertura del libro, y la UI le daba el botón a los dos roles → se podían anotar ventas en un arqueo firmado. Ahora se rechaza con el camino del remedio (↺, del dueño) y la UI lo dice.
- **[B] `rollback_update` / `backup_before_update` sin gate**: desde la consola del WebView se podía volver a la build anterior a F68 (sin roles). El updater sigue funcionando (`get_update_state`, `run_health_check`, `mark_update_ok/failed` sin gate: los usa el arranque).
- **Mayores arreglados:** `getDrawerAdjustments` se tragaba el error y devolvía ceros (el esperado volvía a ser el de A1) y ahora el cierre se bloquea si el ajuste no se pudo leer; el preconteo del cajón arranca en el esperado de verdad (con fondo y gastos, no en lo cobrado); `sinContar` suma el ajuste (acusaba «sin contar» a un cierre perfecto); `close_day` guarda el fondo que USÓ (una sola fuente); el costo del catálogo seguía saliendo por `find_compatible_screens`/`find_compatible_products`/`get_phone_detail`/pedidos (ahora se borra en el origen y es fail-closed con la sesión vencida); `export_daily_report(_xlsx)`, `get_expenses`, `search_payments`, `get_payment_daily_detail`, `get_dashboard_analytics` y las compras quedaron detrás del dueño; la sesión de 12 h ahora se vigila (re-consulta al volver a la ventana y cada minuto: antes, pasadas las 12 h, la plata se anotaba sin autor); `update_service_payment_date` pide dueño si el cobro es de otra persona; el respaldo ya no lleva el hash del PIN y borrar al master «de la casa» resincroniza `settings.pin` (el PIN del master borrado seguía entrando); el bloqueo por intentos se persiste (reiniciar la app ya no lo borra) y `set_user_pin` valida 4 dígitos; textos que contradecían la feature (los digitales «con el valor del sistema», los gastos de HOY al cerrar otro día, el gasto en Bs. impreso con formato de dólar, el botón «Abrir Día» sobre un día cerrado); `session.ts` ahora dice la verdad (`closeOwnCaja`/`cashExpense` en false y `manageSettings` nuevo) y la UI de Servicios esconde impresora/técnicos/quitar categoría a la caja.

**Y una regla que quedó más fuerte que antes:** **el cajón se cuenta SIEMPRE en las dos monedas**, aunque el esperado sea 0 — si en el cajón hay Bs. que el día no explica, la diferencia tiene que verse («sobran Bs. 500,00») en vez de quedar en un 0 asumido (y es lo que mitiga que el fondo de caja sólo se declare en dólares).

**Verificado de nuevo tras los arreglos:** `cargo test --lib` **152/152** · `arqueo_test.ts` **49/49** · `pos_cuadre_test.ts` 66/66 · `session_test.ts` **42/42** · EN VIVO `verify_arqueo_f69.mjs` **37/37 y 39/39** (dos copias) · `verify_sesiones_caja` 28/28 · `verify_tecnico_y_fecha_pago` **41/41** · `verify_smoke_integral` **OK (×2)** · `release_gate` LISTO · `tsc -b` 0 · `oxlint` 0 errores.

---

## F70 — Anular (o corregir) una venta con reverso de stock (2026-09-23)

**Origen:** bloqueante **A3** de `AUDITORIA_ENTREGA.md`. Antes: **no existía `update_sale` ni `delete_sale`** (0 comandos, 0 `DELETE FROM sales`), así que un precio o una cantidad mal tecleados quedaban en la caja de ese día **para siempre** y una pantalla vendida y devuelta **no volvía al stock**; `Help.tsx` afirmaba lo contrario. Y el «botón muerto sin precio» de la auditoría era, medido: `disabled={… || !productName || price <= 0}` — apagado **sin decir por qué**.

**Hecho:** columnas `sales.voided_at`/`void_reason` (apendadas al final, migración idempotente) + **`void_sale(id, motivo)`** transaccional y del **dueño** (`require_owner`): la venta **no se borra** (queda marcada con su motivo y su fecha), **devuelve el stock** con movimiento de inventario de ENTRADA («Anulación de venta»), baja `clients.total_spent`, y escribe el **contra-asiento** en el libro (tipo `venta_anulada`, mismo método/moneda/monto NETO, signo −1, con autor y motivo) **en el día de la VENTA** (no el de hoy). Sólo se puede anular si el **día de la venta está abierto** (un cierre guardado no se recalcula: el error dice el camino ↺ → anular → volver a cerrar). El arqueo (`compute_daily_totals`), el top de productos, el `total_spent`/`sale_count` del cliente, la utilidad (ingreso **y costo**), el dashboard, el buscador de pagos y los reportes **dejan de contarla** (los reportes la exportan marcada «ANULADA: motivo», no la esconden). Regla pura nueva **`src/lib/void-sale.ts`**: impacto con números (de qué caja sale, cuántas unidades vuelven), motivo obligatorio, fila anulada visible y KPIs sin contar lo anulado. UI: botón **«Anular»** por fila (sólo dueño), diálogo con el impacto y el motivo, fila **tachada** con badge y motivo, KPI «N anuladas (no cuentan)»; el formulario de venta **ya no está mudo** (avisa qué falta) y el KPI **«Sin precio»** del Inventario ahora **LISTA** las fichas que no se pueden cobrar.

**Verificado:** `cargo test --lib` **154/154** (3 tests nuevos/ampliados) · `node tools/void_sale_test.ts` **35/35** · **EN VIVO `node tools/verify_anular_venta.mjs` 36/36** (incluye la BASE: stock devuelto, movimiento de entrada, contra-asiento con autor y el arqueo del día bajando exactamente el monto) · regresiones: `verify_smoke_integral` **OK** (su chequeo del botón bloqueado se reescribió a la regla nueva) y `verify_arqueo_f69` **37/37** · `tsc -b` 0 · `oxlint` 0 errores.

---

## F71 — Respaldo y restauración desde la app (2026-09-23)

**Origen:** bloqueante **A2** de `AUDITORIA_ENTREGA.md`: los comandos de respaldo existían pero **ninguna pantalla los llamaba** y la Ayuda decía «copiá registro.db a un USB».

**Hecho:** módulo **`src-tauri/src/backups.rs`** (copia consistente con `VACUUM INTO` —incluye el WAL—, retención de 14 automáticos sin tocar los manuales ni las copias de seguridad, validación fail-closed del candidato, copia de la base ACTUAL antes de restaurar y **restauración diferida al arranque** con marcador, porque pisar el archivo con la base abierta corrompe el WAL), **copia automática al cerrar el día** (no rompe el cierre si falla: el error se muestra), 5 comandos del dueño y la pantalla **Ayuda → «Respaldos»** con el estado a la vista («todavía no hay ningún respaldo: si esta PC se rompe, se pierde todo el negocio»), «Respaldar ahora», carpeta elegible (USB) y restaurar con confirmación que dice qué se pisa. Regla pura `src/lib/backup.ts`. Además, **la Ayuda dejó de mentir**: el respaldo ya no es «copiá un archivo a mano» y «corregir una venta» ahora dice la verdad (se **anula**, con motivo y reverso de stock — F70).

**Verificado:** `cargo test --lib` **157/157** · `node tools/backup_test.ts` **37/37** · **EN VIVO `verify_respaldo.mjs` 17/17** + **`verify_respaldo2.mjs` 7/7** (respaldo real con las mismas filas que la base viva, restauración con reinicio: 0 filas de prueba después, `quick_check` ok) · regresiones: `verify_arqueo_f69` 37/37, `verify_anular_venta` 36/36, `verify_smoke_integral` OK · `tsc -b` 0 · `oxlint` 0 errores.

**SPRINT A COMPLETO (F68 → F71):** sesiones de caja con autor y visibilidad restringida, arqueo del cajón con fondo/gastos y verificación de los digitales, anulación de ventas con reverso de stock, y respaldo/restauración desde la app. Los cuatro huecos que la auditoría marcaba como bloqueantes para entregar al cliente final quedaron cerrados y verificados en vivo.

---

## F72 — El encabezado del Libro Diario, en SUBCATEGORÍAS (2026-09-23)

**Origen:** pedido directo del dueño con la app en la mano: «**arregla toda esta pestaña… está muy larga**». El encabezado metía **nueve botones iguales en una sola fila** al lado del título (Exportar Excel · PIN · Diario · Cierres · Pagos · Gastos · Salud · Movimientos · Personas): no entraba en el ancho de la ventana y no se distinguía una **pestaña** (cambia lo de abajo) de una **acción** (abre un diálogo o descarga un archivo).

**Hecho:** (1) encabezado en **dos niveles** —título con las **acciones del dueño** a su derecha (Exportar Excel · PIN · Personas) y, abajo, la **barra de secciones**—; (2) las secciones agrupadas en **subcategorías rotuladas** y en el orden de uso del día: **CAJA DEL DÍA** (Diario · Cierres) · **PLATA** (Pagos · Gastos) · **CONTROL** (Movimientos · Salud), separadas por una línea; (3) una sola definición con la visibilidad por rol en la regla pura nueva **`src/lib/ledger-nav.ts`** (`LEDGER_NAV`, `LEDGER_ACTIONS`, `ledgerNav(ab)`, `ledgerActions(ab)`, `ledgerTabVisible`, `isLedgerTab`, `ledgerTabLabel`, `ledgerTabGroup`), con las MISMAS capacidades de `session.ts` (F68/F69): un ítem sin permiso no se dibuja y un grupo sin ítems tampoco; (4) el subtítulo dice dónde estás («estás en Caja del día → Diario») y cada pestaña lleva su explicación en el `title`; (5) los rótulos siguen siendo **exactos** (Diario, Cierres, Pagos, Gastos, Salud, Movimientos) porque las verificaciones en vivo buscan los botones por su texto, y `data-tab` se puso en todas las pestañas.

**Medido en vivo (ventana 1200×749):** fila de pestañas **687 px** de 1200 (antes: 9 botones de tamaño completo, ~1140 px para 1110 disponibles → desbordaba), barra **849 px**, **46 px de alto**, **una sola fila**, sin scroll horizontal, ningún rótulo cortado, activa vs inactiva pintadas distinto (2 separadores para 3 grupos). La caja ve **Diario** y **Movimientos** —y nada del dueño— con su rótulo de grupo conservado.

**Verificado:** `node tools/ledger_nav_test.ts` **34/34** · **EN VIVO `node tools/verify_libro_diario_nav.mjs` 48/48** por CDP (2 niveles, subcategorías, las 6 pestañas abriendo SU contenido, nada fuera del ancho, sin scroll horizontal, una fila, sin rótulos cortados, y los accesos de la CAJA con su PIN) · regresión **`verify_arqueo_f69.mjs` 37/37** (incluidas las comprobaciones de visibilidad por sesión del Libro Diario) · `tsc -b` 0 · `oxlint` 0 errores · `npm run build` + `cargo build` ✓ · `harness_security` PASS · `harness_truth` PASS.

**Hallazgo anotado (no arreglado):** el `MoneyInput` (es-VE) toma los dígitos sin separador como **centavos** cuando pasan de dos («145» → «1,45»); en el arqueo está mitigado (la línea viene precargada + «Es el esperado»), pero en un campo vacío es una trampa para el operario. Arreglo sugerido: una línea en `formatMoneyRaw`. Merece su propia feature (lo usan TODOS los montos) y su verificación en vivo. Por lo mismo, el script `verify_arqueo_f69.mjs` ahora escribe el valor **ya formateado** («145,00») en vez del número crudo.

---

## F73 — La máscara de los céntimos (2026-09-25)

**Origen:** hallazgo medido en vivo al cerrar F72: el `MoneyInput` es-VE tomaba los dígitos sin separador como CENTAVOS (`145` → `1,45`; para cargar $145 había que teclear `14500`), en TODOS los campos de dinero (arqueo, gastos, apertura, liquidar Punto) y era la causa real de tres comprobaciones rojas de `verify_arqueo_f69` que parecían del arqueo.

**Hecho:** la máscara pasa a regla pura `src/lib/money.ts` con pruebas (`tools/money_test.ts` 107/107): sin separador los dígitos son la parte ENTERA, con coma/punto son decimales (máx. 2), un punto con 3 dígitos sin coma es separador de MILES al pegar (se va el error de 1000×), separador mostrado siempre la coma, texto vacío/basura = 0 (nunca NaN); + `redondearCentavos`/`redondearBolivar` compartidos. `MoneyInput` sólo enchufa la regla.

**Verificado:** `money_test` 107/107 · EN VIVO `verify_money_input.mjs` **19/19** (lo que se escribe es lo que se muestra; en el arqueo «145» cuenta $145) · `verify_arqueo_f69` 37/37 · `tsc` 0 · `oxlint` 0 errores.

---

## F74 — El IVA configurable, con sus céntimos en Bs. a la tasa BCV (2026-09-25)

**Origen:** pedido del dueño: «incluye el IVA que se pueda activar o desactivar… todo listo para producción, que tome sus centavos sincronizado con la tasa del día BCV», confirmado como **configurable** (incluido o agregado), en **ventas y servicios**, 16% editable, y con «el IVA aplica en bolívares y tenga sus céntimos los montos en base a la tasa BCV en Bs.».

**Hecho:** regla pura `src/lib/iva.ts` (`desgloseIva`, `desgloseGuardado`, `ivaDelPeriodo`, `ivaDeGrupos`, `parseIvaConfig` fail-closed) con invariantes: base + IVA = total al centavo; en Bs. el IVA es el residuo (no queda 1 Bs. de diferencia); el total en Bs. lleva sus céntimos con la tasa del turno y aparte se informa el efectivo al bolívar entero; sin tasa no se inventa ningún número en Bs. Configuración en `settings.tax_config` (`get_tax_config` abierto, `set_tax_config` del dueño con validación) y diálogo **Libro Diario → IVA** con cuenta en vivo. Por fila: `sales.iva_rate/iva_mode` y `services.iva_rate/iva_mode` apendadas al final (migración idempotente; `add_sale` delega en `add_sale_tax` para no tocar 31 llamadores): el monto guardado sigue siendo lo que paga el cliente y la alícuota viaja con la operación. Se ve en la venta, el wizard de servicio, el recibo (BASE + IVA + TOTAL) y el Libro Diario (**IVA del período**: base imponible, IVA, cobrado con IVA y lo exento aparte).

**Verificado:** `iva_test` **60/60** · `money_test` 107/107 · `receipt_acuerdo_test` **92/92** · `cargo test --lib` **158/158** · EN VIVO `verify_iva.mjs` **31/31** · `verify_money_input.mjs` 19/19 · `tsc` 0 · `oxlint` 0 errores · `harness_security` PASS · `harness_truth` PASS.

---

## F75 — El Excel del contador: por método de pago, con referencias e IVA (2026-09-25)

**Origen:** pedido del dueño: «arregla el Excel, todo ordenado y por método de pago; si todo son Pago Móvil tenga su monto y número de referencia; un Excel profesional que mi contador en Venezuela entienda y pueda visualizar rápido». El export tenía 5 hojas planas, sin IVA, sin orden por método y sin conciliación.

**Hecho:** el .xlsx sale con **10 hojas** para el contador: Resumen (total por método y moneda + IVA del período + totales por día), **Por método de pago** (ventas + abonos en una tabla ordenada por método y fecha, con SUBTOTAL que se recalcula al filtrar), **Pago Móvil** (monto + Nº de referencia de cada pago, con total y aviso rojo si falta alguna referencia), Ventas (con base/IVA/total y las ANULADAS marcadas), Servicios (base/IVA/total/abonado/saldo), Pagos y Abonos, **IVA del período** (libro para declarar por alícuota y tipo + detalle de cada operación gravada), Cierres de caja, Gastos (totales separados por moneda) y Movimientos. Formatos de moneda con céntimos, encabezados congelados, autofiltro, impresión apaisada y el nombre del negocio (el del recibo) en el título. La matemática del IVA espeja `src/lib/iva.ts` (base + IVA = total al centavo; la alícuota de CADA fila) y nunca se suman $ y Bs. en una columna.

**Hallazgo arreglado de paso:** una base con NULL en `sales.client_name`/`services.client` hacía fallar `init()` y **la app no abría** (`InvalidColumnType(1, "client_name", Null)`). Ahora los nombres se leen defensivo (NULL = vacío) y la base abre igual, con test Rust `test_null_names_do_not_break_startup`.

**Verificado:** `python tools/export_libro_diario_test.py` **27/27** (genera un .xlsx real y lo inspecciona) · EN VIVO `node tools/verify_export_excel.mjs` **9/9** + `tools/export_libro_diario_check.py` sobre el archivo real (12 cobros ordenados por método; Pago Móvil con REF-1234/REF-5678) · `cargo test --lib` **159/159**.
