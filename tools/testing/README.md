# Testing Quality

Validates test code quality — assertion coverage, .only() detection, test organization.

## Modules

- **test-quality.ts** — Scans test files, classifies (unit/integration/e2e), checks assertions.

## Usage

```bash
tsx tools/cli/index.ts loop test-quality
```
